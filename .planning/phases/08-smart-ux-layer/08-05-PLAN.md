---
phase: 08-smart-ux-layer
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/BOT_DESIGN.md
autonomous: true
---

<objective>
Update architecture documentation to reflect Phase 8 additions.

Purpose: Keep docs/architecture/*.md in sync with the new Smart UX Layer features: structured questions, intent audit logging, deterministic post-filters, and /maro inspect command.

Output: Updated BOT_DESIGN.md with new sections covering all Phase 8 features.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-smart-ux-layer/08-CONTEXT.md
@.planning/phases/08-smart-ux-layer/08-RESEARCH.md

@docs/architecture/BOT_DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BOT_DESIGN.md with Phase 8 features</name>
  <files>docs/architecture/BOT_DESIGN.md</files>
  <action>
Read the full BOT_DESIGN.md first. Then add the following new sections to the architecture document, placed logically near related existing content:

1. **Add to Table of Contents:**
   - 11. [Structured Questions (Smart UX)](#structured-questions-smart-ux)
   - 12. [Intent Audit Logging](#intent-audit-logging)
   - 13. [Deterministic Post-Filters](#deterministic-post-filters)
   - 14. [Observability Tools](#observability-tools)

2. **New section: Structured Questions (Smart UX)** (after Prompts Overview or Safety Guardrails):

Document how CONVERSE mode responses now include structured follow-up questions:
- ConverseLLMResponse extended with `follow_up_questions` field
- Three question types: `choice` (buttons), `confirmation` (yes/no), `open_ended` (text)
- Slack rendering: buttons with "Something else" escape hatch
- Post-selection behavior: original message updated, answer posted as thread message
- LLM prompt guidance for question generation
- Slack Block Kit constraints: 75 char button labels, 25 elements per actions block, 255 char value payload

3. **New section: Intent Audit Logging** (after Two-Stage Intent Classification):

Document the audit trail:
- Every classification persisted: message → pregate → raw LLM → threshold-adjusted → mode
- Fire-and-forget writes (asyncio.Task with strong reference set)
- Table schema: intent_audit_log with monthly partitions
- Key fields: raw_mode/raw_confidence vs classified_mode/classified_confidence
- Query patterns for debugging

4. **New section: Deterministic Post-Filters** (after Two-Stage Intent Classification, before or after Audit Logging):

Document the three-stage classification pipeline:
- Stage 1: PreGates (deterministic pre-routing) — existing
- Stage 2: LLM Router (structured classification) — existing
- Stage 3: Post-Filters (deterministic validation) — NEW
  - Entity existence check: MODIFY with target_entity_id must reference existing entity
  - Invalid entity mention cleanup
  - Graceful degradation on aggregate loading failure
- Update the classification flow diagram if one exists

5. **New section: Observability Tools** (at the end):

Document /maro inspect:
- `/maro inspect` — recent classifications
- `/maro inspect thread <ts>` — thread-specific audit trail
- `/maro inspect stats` — classification distribution
- `/maro inspect downgrades` — threshold downgrade history
- All output ephemeral (debug info)

IMPORTANT formatting rules for this markdown file:
- Standard markdown (not Slack mrkdwn) — **bold**, `code`, ## headings are all fine here
- Use tables, code blocks, and diagrams where helpful
- Keep existing style and tone consistent
- Add cross-references to source files where appropriate
  </action>
  <verify>python -c "
import pathlib
content = pathlib.Path('docs/architecture/BOT_DESIGN.md').read_text()
assert 'Structured Questions' in content
assert 'Intent Audit' in content
assert 'Post-Filters' in content
assert 'Observability' in content or 'inspect' in content
print('All sections present')
"</verify>
  <done>BOT_DESIGN.md updated with all Phase 8 features: structured questions, audit logging, post-filters, observability tools</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] BOT_DESIGN.md contains section on Structured Questions
- [ ] BOT_DESIGN.md contains section on Intent Audit Logging
- [ ] BOT_DESIGN.md contains section on Deterministic Post-Filters
- [ ] BOT_DESIGN.md contains section on Observability Tools
- [ ] Table of contents updated
- [ ] Formatting consistent with existing document style
- [ ] Source file cross-references included
</verification>

<success_criteria>

- BOT_DESIGN.md fully documents Phase 8 features
- New sections are consistent with existing document style
- Classification pipeline documented as 3-stage (pregates → LLM → post-filters)
- All source file paths referenced for traceability
</success_criteria>

<output>
After completion, create `.planning/phases/08-smart-ux-layer/08-05-SUMMARY.md`
</output>
