---
phase: 10-code-review-polish
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/slack/handlers/actions.py
  - src/infrastructure/projections.py
  - src/jira/sync_service.py
autonomous: true
---

<objective>
Decouple Jira notifications from action handlers (ISS-005) — move to event-driven side effects.

Purpose: `handle_deprecate_confirm` and `handle_record_confirm` perform Jira API calls synchronously. If Jira is slow or down, the user waits. This blocks the Slack 3-second ack window and creates poor UX.

Output:
- Action handlers: ack + aggregate mutation + immediate UI feedback only
- Jira notifications triggered asynchronously by DecisionDeprecated / DecisionAmended events via the projection/outbox system
- User gets instant feedback; Jira updates happen in the background
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ISSUES.md

@src/slack/handlers/actions.py
@src/infrastructure/projections.py
@src/infrastructure/event_store.py
@src/jira/sync_service.py
@src/domain/events.py
</context>

<tasks>
## Task 1: Understand current outbox/projection flow

Read:
- `src/infrastructure/event_store.py` — outbox pattern, how events are queued
- `src/infrastructure/projections.py` — how outbox events are consumed, projection apply() dispatch
- Check if there's an outbox consumer/worker that processes pending outbox events

Determine: Where to add Jira notification as an event-driven side effect. Options:
1. Add to existing projection (EntityViewProjection handles DecisionDeprecated/DecisionAmended already)
2. Create a new projection (JiraNotificationProjection) — cleaner separation
3. Use background task spawning from the outbox consumer

## Task 2: Remove Jira calls from action handlers

**File: `src/slack/handlers/actions.py`**

### In `handle_deprecate_confirm`:
- Remove the `sync_service.notify_decision_deprecated(...)` call
- Remove the sync_service import if no longer needed in this handler
- Keep: ack, aggregate mutation, save_events, ADR message update, dashboard update, UI feedback

### In `handle_record_confirm` (confirm_amend_decision branch):
- Remove the `sync_service.notify_decision_amended(...)` call
- Keep: ack, aggregate mutation, save_events, ADR message update, dashboard update, UI feedback

The events are already emitted and persisted — the projection system will handle notifications.

## Task 3: Add Jira notification to projection/outbox consumer

Based on Task 1 discovery, implement event-driven Jira notifications:

**Option A — Add to existing projection:**
Add cases for DecisionDeprecated and DecisionAmended in the projection that call sync_service asynchronously. This keeps it simple but couples projection with external API.

**Option B — New JiraNotificationProjection:**
Create a lightweight projection that only handles DecisionDeprecated + DecisionAmended events, calling sync_service. Registered in the outbox consumer alongside EntityViewProjection.

Choose the approach that best fits the existing codebase architecture. Document the decision.

## Task 4: Ensure fault tolerance

The Jira notification projection must:
- Not crash on Jira failures (try/except with logging)
- Not block other projections
- Not retry indefinitely (log and move on — manual reconciliation if needed)

The existing sync_service methods already have try/except + logger.warning — verify this is preserved.

## Task 5: Run tests

Run `pytest tests/ -x` to verify no regressions.
</tasks>

<verification>
1. `pytest tests/ -x` passes
2. `handle_deprecate_confirm` contains NO Jira sync_service calls
3. `handle_record_confirm` contains NO Jira sync_service calls for amendments
4. DecisionDeprecated events trigger Jira notification via projection/outbox
5. DecisionAmended events trigger Jira notification via projection/outbox
6. Jira failure does not crash the projection or block event processing
</verification>

<success_criteria>
- Action handlers complete within Slack's 3s window regardless of Jira status
- Jira notifications still happen (just asynchronously)
- Existing fault tolerance preserved (Jira failure → log warning, continue)
</success_criteria>

<output>
Updated: src/slack/handlers/actions.py, src/infrastructure/projections.py (or new file), src/jira/sync_service.py
Closed: ISS-005 (handlers do too much)
</output>
