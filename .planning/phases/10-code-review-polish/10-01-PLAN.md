---
phase: 10-code-review-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/slack/handlers/actions.py
  - src/modes/record.py
  - src/slack/blocks/builders.py
autonomous: true
---

<objective>
Fix brittle block parsers (ISS-003) and entity ID action_id convention (ISS-004).

Purpose: Block parsers (`_parse_record_mode_preview`, `_parse_amend_preview`) extract decision data by parsing mrkdwn text — any change to block format silently breaks them. Deprecation buttons embed entity UUID in action_id strings — requires regex matching. Both are maintenance hazards.

Output:
- Decision data stored as JSON in `action.value` — parsers become `json.loads()` instead of string manipulation
- Deprecation/confirm buttons use fixed `action_id` with entity ID in `action.value`
- Block format changes can no longer silently break data flow
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md

@src/slack/handlers/actions.py
@src/modes/record.py
@src/slack/blocks/builders.py
</context>

<tasks>
## Task 1: Store decision data as JSON in action.value for RECORD mode buttons

**File: `src/modes/record.py`**

In `_build_decision_preview_blocks` and `_build_amendment_preview_blocks`:
- Serialize the full decision dict as JSON into the `value` field of each action button (Record Decision, Edit, Cancel, Amend, Record as New)
- The JSON must include: `title`, `decision_type`, `decision`, `rationale`, `alternatives_considered`
- For amendment buttons, also include `entity_id` in the value JSON
- Slack button `value` limit is 2000 chars — sufficient for typical decisions
- Blocks remain purely presentational (no data changes to block structure)

**Verification:** Run existing tests, ensure record.py block builders still produce valid Block Kit

## Task 2: Replace block parsers with JSON deserialization

**File: `src/slack/handlers/actions.py`**

- Delete `_parse_record_mode_preview()` function entirely (lines ~143-204)
- Delete `_parse_amend_preview()` function entirely (lines ~207-263)
- In `handle_record_confirm`:
  - For `confirm_record_decision`: Read decision data from `json.loads(action["value"])` instead of calling `_parse_record_mode_preview(blocks)`
  - For `confirm_amend_decision`: Read decision + entity_id from `json.loads(action["value"])` instead of calling `_parse_amend_preview(blocks)`
  - For `edit_decision`: Read decision data from `json.loads(action["value"])` for the edit modal
  - For `cancel_decision`: No data parsing needed — just update message
- Fallback: if `action["value"]` is missing or invalid JSON, log warning and return error to user

**Verification:** Record single decision via RECORD mode confirm → works. Amend existing decision → works.

## Task 3: Fix deprecation buttons — fixed action_id + entity ID in value

**File: `src/slack/handlers/actions.py`**

### 3a: Update button builders
In `handle_deprecate_decision` (the handler that shows confirmation):
- Change confirmation button from `action_id=f"confirm_deprecate_{entity_id}"` to `action_id="confirm_deprecate"` with `value=str(entity_id)`
- Change cancel button from `action_id=f"cancel_deprecate_{entity_id}"` to `action_id="cancel_deprecate"` with `value=str(entity_id)`

In dashboard builder (wherever the deprecate button is created):
- Change from `action_id=f"deprecate_decision_{entity_id}"` to `action_id="deprecate_decision"` with `value=str(entity_id)`

### 3b: Update patterns and handlers
- Remove `DEPRECATE_DECISION_PATTERN` regex — replace with exact string match: `@app.action("deprecate_decision")`
- Remove `DEPRECATE_CONFIRM_PATTERN` regex — replace with `@app.action(re.compile(r"^(confirm|cancel)_deprecate$"))`
- In `handle_deprecate_decision`: Read entity_id from `action["value"]` instead of regex match on `action_id`
- In `handle_deprecate_confirm`: Read entity_id from `action["value"]` instead of regex match on `action_id`

**Verification:** Deprecate a committed decision → confirmation flow works. Cancel → message updated correctly.

## Task 4: Run tests

Run `pytest tests/ -x` to verify no regressions.
</tasks>

<verification>
1. `pytest tests/ -x` passes
2. No regex patterns remain for entity ID extraction from action_id (except existing entity action pattern for approve/object/discuss)
3. `_parse_record_mode_preview` and `_parse_amend_preview` functions are deleted
4. All button `value` fields contain the data needed by their handler
</verification>

<success_criteria>
- Zero brittle mrkdwn parsers in actions.py
- Deprecation flow uses fixed action_id + value for entity ID
- Existing tests pass
- Block format changes are safe — data flows through `action.value`, not block text
</success_criteria>

<output>
Updated: src/slack/handlers/actions.py, src/modes/record.py
Closed: ISS-003 (brittle parsers), ISS-004 (entity IDs in action_id)
</output>
