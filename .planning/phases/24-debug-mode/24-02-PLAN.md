---
phase: 24-debug-mode
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified: [src/slack/handlers/commands.py, src/slack/onboarding.py]
autonomous: true
---

<objective>
Implement /maro debug slash command with on/off/status/state subcommands and update help messages.

Purpose: Allow users to enable debug mode and inspect internal state via slash commands.
Output: Working /maro debug command with all subcommands, updated help documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-debug-mode/24-01-SUMMARY.md

@src/slack/handlers/commands.py
@src/slack/onboarding.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add debug subcommand to _handle_maro_command_async</name>
  <files>src/slack/handlers/commands.py</files>
  <action>
In _handle_maro_command_async(), add handling for "debug" subcommand:

1. Update the docstring to include debug commands:
   - /maro debug - Show debug status
   - /maro debug on - Enable debug mode
   - /maro debug off - Disable debug mode
   - /maro debug status - Quick health check
   - /maro debug state - Full internal state dump

2. Add elif branch for subcommand == "debug":
   ```python
   elif subcommand == "debug":
       # /maro debug [on|off|status|state]
       debug_action = args[0].lower() if args else "status"
       await _handle_maro_debug(channel, user_id, debug_action, client, say)
   ```

3. Create _handle_maro_debug async function that:
   - For "on": Call DebugStore.enable(), confirm with message
   - For "off": Call DebugStore.disable(), confirm with message
   - For "status": Show quick health check (enabled, who set it, channel mode, counts)
   - For "state": Show full internal dump using _build_debug_state_blocks()

4. Create helper functions:
   - _build_debug_status_blocks(channel_id) -> list[dict]
   - _build_debug_state_blocks(channel_id) -> list[dict]

Status output format:
```
Debug Mode: ON (enabled by @boris 2h ago)
Channel: #project-alpha
Mode: PROJECT
Default Project: SCRUM
Listening: OFF
Active WorkItems: 12 (3 draft, 9 active)
Jira Registry: 47 issues linked
```

State output format (as Slack blocks with sections):
```
=== Channel Context ===
Version: 14
Default Project: SCRUM
...

=== WorkItem Registry ===
DRAFT (3):
  - abc123: "Add login validation" (story)
...

=== Jira Registry ===
Owned: SCRUM-101, SCRUM-102...
...
```
  </action>
  <verify>python -c "from src.slack.handlers.commands import _handle_maro_debug; print('OK')"</verify>
  <done>Debug subcommand routes correctly and handlers are defined</done>
</task>

<task type="auto">
  <name>Task 2: Update help and onboarding to include all slash commands</name>
  <files>src/slack/onboarding.py</files>
  <action>
Update get_help_blocks() in src/slack/onboarding.py to include ALL /maro subcommands:

Current commands to document:
- /maro help - Show this help
- /maro enable - Enable listening in channel
- /maro disable - Disable listening
- /maro status - Show listening status
- /maro track PROJ-123 - Track Jira issue in channel
- /maro untrack PROJ-123 - Remove from tracked
- /maro tracked - List tracked issues
- /maro board - Show/update pinned board
- /maro board hide - Remove pinned board
- /maro sync - Show pending Jira sync changes
- /maro sync --auto - Auto-apply obvious changes
- /maro mode - Show channel mode
- /maro mode project|feature|bugs|ops - Set channel mode
- /maro debug - Show debug status (NEW)
- /maro debug on|off - Enable/disable debug mode (NEW)
- /maro debug state - Show full internal state (NEW)

Group commands logically:
1. Getting Started: help, enable, disable, status
2. Issue Tracking: track, untrack, tracked, board
3. Sync: sync, sync --auto
4. Configuration: mode
5. Debugging: debug, debug on/off, debug state

Ensure the help output is scannable and not overwhelming.
  </action>
  <verify>python -c "from src.slack.onboarding import get_help_blocks; b = get_help_blocks(); print(f'{len(b)} blocks')"</verify>
  <done>Help blocks include all slash commands grouped logically</done>
</task>

<task type="auto">
  <name>Task 3: Create debug state formatting helpers</name>
  <files>src/slack/blocks/debug.py</files>
  <action>
Create src/slack/blocks/debug.py with formatting functions:

1. build_debug_status_blocks(
     enabled: bool,
     set_by: str | None,
     set_at: datetime | None,
     channel_mode: str,
     default_project: str | None,
     listening_enabled: bool,
     workitem_counts: dict[str, int],
     jira_registry_count: int,
   ) -> list[dict]

2. build_debug_state_blocks(
     channel_context: dict,
     workitems: list[dict],
     jira_registry: list[dict],
     recent_activity: dict,
   ) -> list[dict]

3. build_debug_output_blocks(
     collector: DebugCollector,
     truncate_prompts: int = 300,
   ) -> tuple[list[dict], str | None]

   Returns (blocks, full_content_for_file).
   If LLM prompts exceed truncate_prompts, full_content is non-None.

Use Slack block kit formatting:
- Section blocks for main content
- Context blocks for metadata
- Dividers between sections
- Code blocks for JSON/structured data

Export from src/slack/blocks/__init__.py.
  </action>
  <verify>python -c "from src.slack.blocks.debug import build_debug_status_blocks; print('OK')"</verify>
  <done>Debug block builders are importable and functional</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/maro debug` shows status
- [ ] `/maro debug on` enables debug mode
- [ ] `/maro debug off` disables debug mode
- [ ] `/maro debug state` shows internal state
- [ ] `/maro help` shows all slash commands including debug
- [ ] No import errors or syntax errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Debug slash command works with all subcommands
- Help includes all slash commands
</success_criteria>

<output>
After completion, create `.planning/phases/24-debug-mode/24-02-SUMMARY.md`
</output>
