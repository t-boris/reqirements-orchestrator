---
phase: 24-debug-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/models.py, src/db/debug_store.py, src/debug/collector.py]
autonomous: true
---

<objective>
Create debug mode foundation: data models, database store, and debug context collector.

Purpose: Enable per-channel debug mode persistence and structured debug data collection.
Output: DebugModeConfig model, DebugStore, and DebugCollector dataclass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/db/models.py
@src/db/channel_mode_store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DebugModeConfig model to models.py</name>
  <files>src/db/models.py</files>
  <action>
Add DebugModeConfig Pydantic model to src/db/models.py with fields:
- id: str (UUID)
- channel_id: str (unique)
- enabled: bool (default False)
- verbosity: str (default "full") - for future use: "minimal", "standard", "full"
- set_by: str (user ID who enabled/disabled)
- set_at: datetime
- created_at: datetime
- updated_at: datetime

Place it after ChannelModeConfig model for logical grouping.
  </action>
  <verify>python -c "from src.db.models import DebugModeConfig; print('OK')"</verify>
  <done>DebugModeConfig model importable and has all required fields</done>
</task>

<task type="auto">
  <name>Task 2: Create DebugStore with CRUD operations</name>
  <files>src/db/debug_store.py</files>
  <action>
Create src/db/debug_store.py with DebugStore class following the pattern in channel_mode_store.py:
- __init__(conn: AsyncConnection)
- async create_tables() - CREATE TABLE IF NOT EXISTS debug_mode with channel_id UNIQUE
- async get(channel_id) -> Optional[DebugModeConfig]
- async is_enabled(channel_id) -> bool
- async enable(channel_id, user_id, verbosity="full") -> DebugModeConfig
- async disable(channel_id, user_id) -> DebugModeConfig
- async _set(channel_id, enabled, user_id, verbosity) -> DebugModeConfig (internal upsert)

Table schema:
```sql
CREATE TABLE IF NOT EXISTS debug_mode (
    id UUID PRIMARY KEY,
    channel_id TEXT UNIQUE NOT NULL,
    enabled BOOLEAN NOT NULL DEFAULT FALSE,
    verbosity TEXT NOT NULL DEFAULT 'full',
    set_by TEXT NOT NULL,
    set_at TIMESTAMPTZ NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
)
```

Use the same patterns as ChannelModeStore: uuid4 for IDs, ON CONFLICT DO UPDATE for upsert.
  </action>
  <verify>python -c "from src.db.debug_store import DebugStore; print('OK')"</verify>
  <done>DebugStore importable with all CRUD methods defined</done>
</task>

<task type="auto">
  <name>Task 3: Create DebugCollector dataclass</name>
  <files>src/debug/collector.py</files>
  <action>
Create src/debug/ directory and src/debug/collector.py with:

1. DebugEntry dataclass for individual debug entries:
   - timestamp: datetime
   - category: str (e.g., "intent", "duplicates", "decision", "llm", "error")
   - title: str
   - data: dict
   - duration_ms: Optional[int]

2. LLMCall dataclass for LLM call tracking:
   - model: str
   - prompt: str
   - response: str
   - tokens_in: int
   - tokens_out: int
   - duration_ms: int

3. DebugCollector class:
   - entries: list[DebugEntry]
   - llm_calls: list[LLMCall]
   - start_time: datetime
   - add_entry(category, title, data, duration_ms=None)
   - add_llm_call(model, prompt, response, tokens_in, tokens_out, duration_ms)
   - add_error(error: Exception, context: str)
   - get_total_duration_ms() -> int
   - to_slack_blocks(truncate_prompts=300) -> list[dict] - format as Slack blocks
   - to_file_content() -> str - full content for .txt attachment

The to_slack_blocks method should:
- Create sections for each category (Intent, Duplicates, Decision, LLM Calls)
- Truncate LLM prompts/responses to 300 chars with "..." suffix
- Format timing information
- Use Slack mrkdwn formatting

Create src/debug/__init__.py that exports DebugCollector.
  </action>
  <verify>python -c "from src.debug import DebugCollector; c = DebugCollector(); print('OK')"</verify>
  <done>DebugCollector importable with all methods defined</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from src.db.models import DebugModeConfig"` succeeds
- [ ] `python -c "from src.db.debug_store import DebugStore"` succeeds
- [ ] `python -c "from src.debug import DebugCollector"` succeeds
- [ ] No import errors or syntax errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- DebugModeConfig, DebugStore, and DebugCollector are importable and functional
</success_criteria>

<output>
After completion, create `.planning/phases/24-debug-mode/24-01-SUMMARY.md`
</output>
