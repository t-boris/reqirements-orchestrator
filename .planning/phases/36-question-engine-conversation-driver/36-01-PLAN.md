---
phase: 36-question-engine-conversation-driver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/question.py
  - src/schemas/task_plan.py
autonomous: true
---

<objective>
Add QuestionTask schema extending the TaskPlan model with question-specific fields.

Purpose: Questions become first-class tasks in TaskPlan with typed question types, target fields, options, and status tracking.
Output: QuestionTask model integrated into existing Task schema, QuestionType enum, question-specific fields.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-question-engine-conversation-driver/36-CONTEXT.md

@src/schemas/task_plan.py
@src/skills/ask_user.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuestionType enum and QuestionTask model</name>
  <files>src/schemas/question.py</files>
  <action>
Create new src/schemas/question.py with:

1. QuestionType enum with 4 values:
   - CONFIRM_SCOPE — Choice from known options (parent selection, scope)
   - COLLECT_FIELD — Get specific field value (acceptance criteria, title)
   - RESOLVE_CONFLICT — Pick A or B for conflict resolution
   - ASK_USER — Freeform fallback when no template fits

2. QuestionStatus enum with 3 values:
   - PENDING — Not yet answered
   - ANSWERED — User provided answer
   - SKIPPED — Bypassed due to budget or user choice

3. QuestionTask model (Pydantic BaseModel):
   - question_id: str (UUID)
   - question_type: QuestionType
   - question_text: str (the actual question to display)
   - target_field: Optional[str] — For COLLECT_FIELD, which draft field we're filling
   - options: Optional[list[QuestionOption]] — For CONFIRM_SCOPE/RESOLVE_CONFLICT
   - priority: int — Lower = ask first (default 100)
   - status: QuestionStatus (default PENDING)
   - answer: Optional[str] — User's answer once provided
   - answered_at: Optional[datetime]
   - answered_by: Optional[str] — user_id

4. QuestionOption model:
   - option_id: str — For button value
   - label: str — Button text
   - description: Optional[str] — Longer description
   - value: Any — The actual value to apply
   - is_recommended: bool = False — Highlight as recommended

Follow existing Pydantic patterns from task_plan.py (Field defaults, validators).
  </action>
  <verify>python -c "from src.schemas.question import QuestionType, QuestionTask, QuestionOption; print('OK')"</verify>
  <done>QuestionType, QuestionTask, QuestionOption models importable and validated</done>
</task>

<task type="auto">
  <name>Task 2: Extend Task model with question fields</name>
  <files>src/schemas/task_plan.py</files>
  <action>
Extend the existing Task model in task_plan.py to support question tasks:

1. Add optional question_task field to Task model:
   ```python
   question_task: Optional["QuestionTask"] = None  # For question-type tasks
   ```

2. Add is_question property:
   ```python
   @property
   def is_question(self) -> bool:
       """Check if this task is a question task."""
       return self.question_task is not None
   ```

3. Add helper method to create question task:
   ```python
   @staticmethod
   def create_question(
       mode: SuperMode,
       intent: Intent,
       question_task: "QuestionTask",
       depends_on: list[str] = None,
   ) -> "Task":
       """Factory method to create a question task."""
   ```

4. Import QuestionTask at top (handle forward reference properly).

Do NOT modify existing Task fields or behavior — only add new optional fields.
  </action>
  <verify>python -c "from src.schemas.task_plan import Task; t = Task(mode='CHAT', intent='DISCUSSION', title='test'); print(t.is_question)"</verify>
  <done>Task model has question_task field, is_question property, and create_question factory</done>
</task>

<task type="auto">
  <name>Task 3: Export schemas and add tests</name>
  <files>src/schemas/__init__.py</files>
  <action>
1. Add exports to src/schemas/__init__.py:
   ```python
   from src.schemas.question import (
       QuestionType,
       QuestionStatus,
       QuestionTask,
       QuestionOption,
   )
   ```

2. Verify imports work correctly with circular reference handling.

3. Create a simple validation test by importing and creating instances:
   - QuestionTask with CONFIRM_SCOPE type and options
   - QuestionTask with COLLECT_FIELD type and target_field
   - Task with question_task set
  </action>
  <verify>python -c "from src.schemas import QuestionType, QuestionTask, QuestionOption; q = QuestionTask(question_type=QuestionType.CONFIRM_SCOPE, question_text='Test?'); print(q.question_id)"</verify>
  <done>All question schemas exported from src.schemas and instantiable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -c "from src.schemas.question import QuestionType, QuestionTask" succeeds
- [ ] python -c "from src.schemas.task_plan import Task; Task.create_question" succeeds
- [ ] QuestionTask validates correctly with all QuestionType values
- [ ] Task.is_question returns True when question_task is set
</verification>

<success_criteria>

- All tasks completed
- QuestionType enum with 4 values
- QuestionTask model with all required fields
- Task model extended with question support
- No import errors or circular dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/36-question-engine-conversation-driver/36-01-SUMMARY.md`
</output>
