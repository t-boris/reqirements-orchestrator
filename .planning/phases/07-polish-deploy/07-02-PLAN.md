---
phase: 07-polish-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.env.example, .deploy.env.example, .gitignore]
autonomous: true
---

<objective>
Create comprehensive environment configuration files.

Purpose: Document all required environment variables for development and deployment.
Output: Complete .env.example and .deploy.env.example files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config.py

Reference old project env files (adapt for 2.0):
- /Users/boris/github.com/reqirements-orchestrator-old/.env.example
- /Users/boris/github.com/reqirements-orchestrator-old/.deploy.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive .env.example</name>
  <files>.env.example</files>
  <action>
Create .env.example with all required configuration, organized by section:

Sections:
1. LLM Configuration
   - GOOGLE_API_KEY (recommended for production)
   - OPENAI_API_KEY (optional)
   - ANTHROPIC_API_KEY (optional)
   - DEFAULT_LLM_MODEL (default: gemini-2.0-flash)

2. Slack Configuration
   - SLACK_BOT_TOKEN (xoxb-...)
   - SLACK_APP_TOKEN (xapp-... for Socket Mode)
   - SLACK_SIGNING_SECRET

3. Jira Configuration
   - JIRA_URL (https://your-domain.atlassian.net)
   - JIRA_USER (email)
   - JIRA_API_TOKEN
   - JIRA_DEFAULT_PROJECT (optional)
   - JIRA_DRY_RUN (default: false)

4. Database Configuration
   - DATABASE_URL (postgresql://...)
   - POSTGRES_PASSWORD (for docker-compose)

5. Application Settings
   - ENVIRONMENT (development|staging|production)
   - DEBUG (true|false)
   - LOG_LEVEL (DEBUG|INFO|WARNING|ERROR)

Include helpful comments explaining each variable.
  </action>
  <verify>cat .env.example | head -30</verify>
  <done>.env.example has all required sections</done>
</task>

<task type="auto">
  <name>Task 2: Create .deploy.env.example</name>
  <files>.deploy.env.example</files>
  <action>
Create .deploy.env.example for GCP deployment:

Variables:
- GCP_PROJECT (required)
- GCE_INSTANCE (required, default: maro-vm)
- GCE_ZONE (default: us-central1-a)
- REGION (default: us-central1)
- REPO (default: maro)

Include comments explaining each variable and usage.
  </action>
  <verify>cat .deploy.env.example</verify>
  <done>.deploy.env.example has deployment configuration</done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore</name>
  <files>.gitignore</files>
  <action>
Ensure .gitignore includes:
- .env (actual env file)
- .deploy.env (actual deploy config)
- *.pyc, __pycache__
- .pytest_cache
- .hypothesis
- .mypy_cache
- .ruff_cache
- *.egg-info
- dist/
- build/
- .idea/
- .vscode/
- *.log

Keep existing entries, only add missing ones.
  </action>
  <verify>grep -E "\.env$|\.deploy\.env$" .gitignore</verify>
  <done>.gitignore updated with deployment files</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .env.example has all sections
- [ ] .deploy.env.example has GCP config
- [ ] .gitignore excludes sensitive files
- [ ] Comments are clear and helpful
</verification>

<success_criteria>
- All env example files exist
- Documentation is comprehensive
- New developers can configure quickly
- Sensitive files excluded from git
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deploy/07-02-SUMMARY.md`
</output>
