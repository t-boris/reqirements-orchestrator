---
phase: 07-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Dockerfile, docker-compose.yml, cloudbuild.yaml, deploy.sh]
autonomous: true
---

<objective>
Create Docker deployment files based on existing old project configuration.

Purpose: Enable production deployment to GCE VM via Cloud Build.
Output: Working Dockerfile, docker-compose.yml, cloudbuild.yaml, deploy.sh adapted for MARO 2.0.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.py

Reference old project deployment files (do NOT copy verbatim - adapt for 2.0):
- /Users/boris/github.com/reqirements-orchestrator-old/Dockerfile
- /Users/boris/github.com/reqirements-orchestrator-old/docker-compose.yml
- /Users/boris/github.com/reqirements-orchestrator-old/cloudbuild.yaml
- /Users/boris/github.com/reqirements-orchestrator-old/deploy.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile</name>
  <files>Dockerfile</files>
  <action>
Create multi-stage Dockerfile based on old project pattern but adapted for 2.0:

Key differences from old:
- Use python:3.12-slim (not 3.11)
- Entry point: `python -m uvicorn src.main:app --host 0.0.0.0 --port 8000`
- Keep the same multi-stage pattern (builder + runtime)
- Keep health check on port 8000 (matches src/main.py)
- Keep non-root user (maro)
- Keep curl for health checks

Structure:
```dockerfile
# Stage 1: Builder
FROM python:3.12-slim AS builder
# Install build deps, copy pyproject.toml and src, pip install

# Stage 2: Runtime
FROM python:3.12-slim AS runtime
# Copy venv from builder, create non-root user, expose 8000
# HEALTHCHECK with curl to /health
# CMD uvicorn
```
  </action>
  <verify>docker build -t maro:test . 2>&1 | tail -5</verify>
  <done>Dockerfile builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Create docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
Create docker-compose.yml based on old project pattern:

Key services:
1. postgres: PostgreSQL 16-alpine with:
   - POSTGRES_USER=maro, POSTGRES_PASSWORD from env, POSTGRES_DB=maro
   - Volume for data persistence
   - Health check (pg_isready)
   - Memory limit 256M

2. bot: MARO application with:
   - Build from Dockerfile
   - Depends on postgres (healthy)
   - env_file: .env
   - DATABASE_URL pointing to postgres container
   - Port 8000:8000
   - Health check on /health
   - Memory limit 512M

Volume: postgres_data
  </action>
  <verify>docker-compose config</verify>
  <done>docker-compose.yml validates</done>
</task>

<task type="auto">
  <name>Task 3: Create cloudbuild.yaml and deploy.sh</name>
  <files>cloudbuild.yaml, deploy.sh</files>
  <action>
Create Cloud Build and deployment script based on old project:

cloudbuild.yaml:
- Single build step with docker
- Push to Artifact Registry
- Substitutions for REGION and REPO

deploy.sh:
- Source .deploy.env for config
- Required vars: GCP_PROJECT, GCE_INSTANCE
- Optional vars: GCE_ZONE (default us-central1-a), REGION (default us-central1), REPO (default maro)
- Step 1: Build via Cloud Build (gcloud builds submit)
- Step 2: Deploy via SSH (docker-compose pull && up -d)
- Make executable (chmod +x)
  </action>
  <verify>cat cloudbuild.yaml && test -x deploy.sh && echo "OK"</verify>
  <done>cloudbuild.yaml and deploy.sh exist and are valid</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Dockerfile builds successfully
- [ ] docker-compose config validates
- [ ] cloudbuild.yaml is valid YAML
- [ ] deploy.sh is executable
- [ ] All files follow patterns from old project
</verification>

<success_criteria>
- All deployment files exist
- Dockerfile uses python:3.12-slim
- Port 8000 used consistently
- Entry point is uvicorn src.main:app
- Ready for deployment (pending .env configuration)
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-deploy/07-01-SUMMARY.md`
</output>
