---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/domain/events.py, src/infrastructure/serialization.py]
autonomous: true
---

<objective>
Implement event base classes and all entity events from spec Part 4.

Purpose: Define the event vocabulary that captures all state changes.
Output: Complete event hierarchy with serialization support and schema versioning.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@docs/maro_2_0.md (Part 4: Event Sourcing)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create event base class with schema versioning</name>
  <files>src/domain/events.py</files>
  <action>
Create base event class combining spec 4.1 with RESEARCH.md schema versioning:

```python
from pydantic import BaseModel, ConfigDict, Field
from datetime import datetime
from uuid import uuid4
from typing import ClassVar

class DomainEvent(BaseModel):
    """Base for all domain events with schema versioning."""
    model_config = ConfigDict(frozen=True)

    # From RESEARCH.md - schema versioning from day one
    schema_version: ClassVar[int] = 1

    # From spec 4.1
    event_id: str = Field(default_factory=lambda: str(uuid4()))
    aggregate_id: ChannelId  # Channel is aggregate root
    timestamp: datetime = Field(default_factory=datetime.utcnow)
    actor_id: UserId
    version: int  # Sequence within aggregate

    # From RESEARCH.md - correlation/causation for tracing
    correlation_id: str | None = None
    causation_id: str | None = None

    @property
    def event_type(self) -> str:
        return self.__class__.__name__

    def model_dump_with_type(self) -> dict:
        """Serialize with event_type and schema_version."""
        data = self.model_dump()
        data["event_type"] = self.event_type
        data["schema_version"] = self.schema_version
        return data
```

Use Pydantic v2 with frozen=True for immutability.
  </action>
  <verify>python -c "from src.domain.events import DomainEvent; print(DomainEvent.model_fields)"</verify>
  <done>Base event class with schema versioning defined</done>
</task>

<task type="auto">
  <name>Task 2: Implement entity events (WorkItem, Decision)</name>
  <files>src/domain/events.py (append)</files>
  <action>
Add entity events from spec 4.2:

```python
# === Work Item Events ===

class WorkItemDrafted(DomainEvent):
    """Work item draft created in thread."""
    entity_id: EntityId
    thread_ts: ThreadTs
    content: WorkItemContent

class WorkItemProposed(DomainEvent):
    """Work item proposed for approval."""
    entity_id: EntityId
    canonical_message_ts: str

class WorkItemApproved(DomainEvent):
    """Work item approved."""
    entity_id: EntityId
    approved_by: UserId

class WorkItemCommitted(DomainEvent):
    """Work item committed to Jira."""
    entity_id: EntityId
    jira_key: JiraKey

class WorkItemUpdated(DomainEvent):
    """Work item content updated."""
    entity_id: EntityId
    changes: dict[str, Any]
    reason: str

# === Decision Events ===

class DecisionRecorded(DomainEvent):
    entity_id: EntityId
    thread_ts: ThreadTs
    content: DecisionContent

class DecisionProposed(DomainEvent):
    entity_id: EntityId
    canonical_message_ts: str

class DecisionApproved(DomainEvent):
    entity_id: EntityId
    approved_by: UserId

class DecisionCommitted(DomainEvent):
    entity_id: EntityId
    jira_key: JiraKey
    field_path: str

class DecisionDeprecated(DomainEvent):
    entity_id: EntityId
    superseded_by: EntityId | None
    reason: str

# === Conflict Events ===

class ConflictDetected(DomainEvent):
    conflict_type: str
    entity_a_id: EntityId
    entity_b_id: EntityId | None
    jira_key: JiraKey | None
    description: str

class ConflictResolved(DomainEvent):
    conflict_id: str
    resolution_type: str
    resolved_by: UserId
    outcome: str
```

Follow spec exactly for field names and types.
  </action>
  <verify>python -c "from src.domain.events import WorkItemDrafted, DecisionRecorded, ConflictDetected"</verify>
  <done>All entity events defined per spec 4.2</done>
</task>

<task type="auto">
  <name>Task 3: Implement process/plan events and event registry</name>
  <files>src/domain/events.py (append), src/infrastructure/serialization.py</files>
  <action>
Add process events from spec 4.3:

```python
# === Process/Plan Events ===

class ProcessStarted(DomainEvent):
    process_id: str
    process_type: str
    thread_ts: ThreadTs

class ProcessStageCompleted(DomainEvent):
    process_id: str
    stage_name: str
    outputs: dict[str, Any]

class ProcessCompleted(DomainEvent):
    process_id: str
    final_outputs: dict[str, Any]

class PlanCreated(DomainEvent):
    plan_id: str
    items: list[dict]
    source_process_id: str | None

class PlanItemCompleted(DomainEvent):
    plan_id: str
    item_index: int
    result: dict[str, Any]

class PlanCompleted(DomainEvent):
    plan_id: str
```

Create event registry in src/infrastructure/serialization.py:
```python
EVENT_REGISTRY: dict[str, type[DomainEvent]] = {
    "WorkItemDrafted": WorkItemDrafted,
    "WorkItemProposed": WorkItemProposed,
    # ... all events
}

def serialize_event(event: DomainEvent) -> dict:
    """Serialize event for storage."""
    return event.model_dump_with_type()

def deserialize_event(data: dict) -> DomainEvent:
    """Deserialize event from storage."""
    event_type = data.pop("event_type")
    data.pop("schema_version", None)  # Handled by upcasting
    cls = EVENT_REGISTRY[event_type]
    return cls.model_validate(data)
```
  </action>
  <verify>python -c "from src.infrastructure.serialization import EVENT_REGISTRY, serialize_event; print(len(EVENT_REGISTRY))"</verify>
  <done>All events defined, registry complete for serialization</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All events can be instantiated with valid data
- [ ] Events are frozen (immutable)
- [ ] serialize_event produces valid JSON-compatible dict
- [ ] deserialize_event reconstructs events correctly
- [ ] Event registry contains all event types
</verification>

<success_criteria>
- All tasks completed
- Events match spec Part 4.2 and 4.3 exactly
- Schema versioning in place from day one
- Correlation/causation IDs for tracing
- Serialization round-trips correctly
- Ready for EventStore implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
