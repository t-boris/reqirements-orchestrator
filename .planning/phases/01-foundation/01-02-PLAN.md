---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/domain/types.py, src/domain/entities.py, src/domain/content.py]
autonomous: true
---

<objective>
Implement core domain types and entity model from spec Part 3.

Purpose: Define the type system that makes illegal states unrepresentable.
Output: Complete domain model with value objects, content types, and unified entity sum types.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@docs/maro_2_0.md (Part 3: Domain Model)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create value objects and core types</name>
  <files>src/domain/types.py</files>
  <action>
Implement types from spec 3.1:

```python
# Value Objects (NewType or str subclass as in spec)
ChannelId = NewType("ChannelId", str)  # Slack channel ID
ThreadTs = NewType("ThreadTs", str)    # Slack thread timestamp
UserId = NewType("UserId", str)        # Slack user ID
JiraKey = NewType("JiraKey", str)      # Jira issue key (PROJ-123)
Version = NewType("Version", int)      # Entity version

class EntityId(str):
    """Unique entity identifier."""
    @classmethod
    def generate(cls) -> "EntityId":
        return cls(str(uuid4()))

# Enums
class EntityLifecycle(str, Enum):
    DRAFT = "draft"
    PROPOSED = "proposed"
    APPROVED = "approved"
    COMMITTED = "committed"
    DEPRECATED = "deprecated"

class EntityType(str, Enum):
    WORK_ITEM = "work_item"
    DECISION = "decision"
    ARTIFACT = "artifact"

class SyncStatus(str, Enum):
    PENDING = "pending"
    SYNCED = "synced"
    CONFLICT = "conflict"
```

Use Pydantic BaseModel for complex types. Follow spec exactly for names.
  </action>
  <verify>python -c "from src.domain.types import EntityId, EntityLifecycle, ChannelId; print(EntityId.generate())"</verify>
  <done>All value objects and enums importable and working</done>
</task>

<task type="auto">
  <name>Task 2: Create content types (WorkItem, Decision)</name>
  <files>src/domain/content.py</files>
  <action>
Implement content types from spec 3.4 and 3.5:

```python
class IssueType(str, Enum):
    EPIC = "epic"
    STORY = "story"
    TASK = "task"
    BUG = "bug"
    SPIKE = "spike"

class WorkItemContent(BaseModel):
    model_config = ConfigDict(frozen=True)

    issue_type: IssueType
    title: str
    description: str = ""
    acceptance_criteria: list[str] = Field(default_factory=list)
    constraints: list[str] = Field(default_factory=list)
    parent_id: EntityId | None = None
    children_ids: list[EntityId] = Field(default_factory=list)

    def validate_content(self) -> list[str]:
        """Return validation errors."""
        # Implement validation from spec

class DecisionType(str, Enum):
    ARCHITECTURE = "architecture"
    SCOPE = "scope"
    CONSTRAINT = "constraint"
    PRIORITY = "priority"
    PROCESS = "process"
    STRUCTURE = "structure"

class DecisionContent(BaseModel):
    model_config = ConfigDict(frozen=True)

    decision_type: DecisionType
    title: str
    description: str
    rationale: str = ""
    alternatives_considered: list[str] = Field(default_factory=list)
    affects_entities: list[EntityId] = Field(default_factory=list)
```

Also implement Attribution, Modification, Approval, Objection from spec 3.6, 3.9.
Also implement JiraLink from spec 3.7.
  </action>
  <verify>python -c "from src.domain.content import WorkItemContent, DecisionContent, IssueType; wi = WorkItemContent(issue_type=IssueType.STORY, title='Test'); print(wi)"</verify>
  <done>All content types defined and frozen (immutable)</done>
</task>

<task type="auto">
  <name>Task 3: Create unified entity sum types</name>
  <files>src/domain/entities.py</files>
  <action>
Implement entity sum types from spec 3.8:

```python
class DraftEntity(BaseModel):
    """Entity being formed — no Jira link possible."""
    model_config = ConfigDict(frozen=True)

    id: EntityId
    entity_type: EntityType
    channel_id: ChannelId
    thread_ts: ThreadTs
    content: WorkItemContent | DecisionContent
    attribution: Attribution
    version: Version = Version(1)

class ProposedEntity(BaseModel):
    """Entity proposed for approval — has canonical message."""
    # ... as in spec

class ApprovedEntity(BaseModel):
    """Entity approved — ready for Jira projection."""
    # ... as in spec

class CommittedEntity(BaseModel):
    """Entity committed to Jira — has required JiraLink."""
    # ... jira_link is required, not optional

class DeprecatedEntity(BaseModel):
    """Entity superseded — for decisions only."""
    # ... as in spec
```

Define the union type:
```python
Entity = DraftEntity | ProposedEntity | ApprovedEntity | CommittedEntity | DeprecatedEntity
```

Implement get_lifecycle() function from spec.
  </action>
  <verify>python -c "from src.domain.entities import DraftEntity, Entity, get_lifecycle; from src.domain.types import *; from src.domain.content import *"</verify>
  <done>Sum types enforce illegal states unrepresentable pattern</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All types can be imported from src.domain
- [ ] mypy src/domain/ passes (optional but recommended)
- [ ] Entity union type works with isinstance checks
- [ ] Content validation methods work correctly
- [ ] All models are frozen (immutable)
</verification>

<success_criteria>
- All tasks completed
- Domain model matches spec Part 3 exactly
- Types are immutable (frozen=True)
- EntityId generates valid UUIDs
- Content validation works
- Ready for event implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
