---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/__init__.py, src/domain/__init__.py, src/infrastructure/__init__.py, src/application/__init__.py, .env.example, alembic.ini, alembic/env.py]
autonomous: true
---

<objective>
Set up Python project scaffolding with FastAPI, Pydantic, and database tooling.

Purpose: Create the foundational project structure that all subsequent work builds upon.
Output: Working Python project with dependencies installed and basic structure in place.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@docs/maro_2_0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python project with pyproject.toml</name>
  <files>pyproject.toml, .python-version</files>
  <action>
Create pyproject.toml with:
- Name: maro
- Python requires: >=3.12
- Dependencies:
  - fastapi>=0.109.0
  - uvicorn[standard]>=0.27.0
  - pydantic>=2.6.0
  - asyncpg>=0.29.0
  - sqlalchemy[asyncio]>=2.0.25
  - eventsourcing>=9.5.0
  - eventsourcing-sqlalchemy>=0.6.0
  - orjson>=3.9.0
  - alembic>=1.13.0
  - python-dotenv>=1.0.0
- Dev dependencies:
  - pytest>=8.0.0
  - pytest-asyncio>=0.23.0
  - hypothesis>=6.98.0
  - ruff>=0.2.0
  - mypy>=1.8.0

Create .python-version with "3.12"

Use PEP 621 format with [project] section, not setup.py style.
  </action>
  <verify>python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))" succeeds</verify>
  <done>Valid pyproject.toml exists with all required dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create project directory structure</name>
  <files>src/__init__.py, src/domain/__init__.py, src/infrastructure/__init__.py, src/application/__init__.py, tests/__init__.py</files>
  <action>
Create directory structure following spec (Part 2.1):
```
src/
├── __init__.py
├── domain/           # Part 3: Domain Model
│   └── __init__.py
├── infrastructure/   # Part 4: Event Sourcing
│   └── __init__.py
└── application/      # Services layer
    └── __init__.py
tests/
└── __init__.py
alembic/
└── (will be created by alembic init)
```

Each __init__.py should be empty for now (no premature exports).
  </action>
  <verify>ls -la src/domain src/infrastructure src/application tests</verify>
  <done>All directories created with __init__.py files</done>
</task>

<task type="auto">
  <name>Task 3: Configure Alembic for migrations</name>
  <files>alembic.ini, alembic/env.py, alembic/versions/.gitkeep</files>
  <action>
Initialize Alembic with: alembic init alembic

Then modify alembic/env.py:
- Add async support using run_migrations_online with async connection
- Import settings from environment (POSTGRES_URL)
- Configure for PostgreSQL with asyncpg driver

Modify alembic.ini:
- Set sqlalchemy.url to empty (will be set from env)
- Set script_location = alembic

Create alembic/versions/.gitkeep to preserve empty directory.
  </action>
  <verify>alembic --help shows available commands</verify>
  <done>Alembic configured for async PostgreSQL migrations</done>
</task>

<task type="auto">
  <name>Task 4: Create environment configuration</name>
  <files>.env.example, src/config.py</files>
  <action>
Create .env.example with all required variables:
```
# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=maro
POSTGRES_PASSWORD=maro
POSTGRES_DBNAME=maro

# Event Store Settings
IS_SNAPSHOTTING_ENABLED=true
SNAPSHOTTING_INTERVAL=500

# Application
LOG_LEVEL=INFO
```

Create src/config.py using Pydantic Settings:
- Load from environment with .env file support
- Define Settings class with all configuration
- Use pydantic-settings for validation
- Export get_settings() function with caching
  </action>
  <verify>python -c "from src.config import get_settings; print(get_settings())" with .env file</verify>
  <done>Configuration loads from environment with validation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -e .` succeeds
- [ ] All directories exist with proper structure
- [ ] `alembic --help` works
- [ ] Configuration loads successfully
- [ ] No import errors from any src module
</verification>

<success_criteria>
- All tasks completed
- Project installable as package
- Alembic configured for async PostgreSQL
- Configuration validated via Pydantic
- Ready for domain type implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
