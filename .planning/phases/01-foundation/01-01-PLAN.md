---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/__init__.py, src/config/__init__.py, src/schemas/__init__.py]
autonomous: true
---

<objective>
Set up Python project structure with pyproject.toml and package layout.

Purpose: Establish the foundation for all subsequent development with proper dependency management.
Output: Working Python package structure with all dependencies installable via `pip install -e .`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with dependencies</name>
  <files>pyproject.toml</files>
  <action>Create pyproject.toml with:
- name: "jira-analyst-bot"
- version: "0.1.0"
- python: ">=3.10"
- dependencies:
  - langgraph
  - langchain-core
  - langchain-google-genai
  - slack-bolt
  - pydantic>=2.0
  - pydantic-settings
  - atlassian-python-api
  - psycopg2-binary (use binary for easier install)
  - python-dotenv
- dev dependencies:
  - pytest
  - ruff (linter)

Use modern pyproject.toml format with [project] section, not setup.py. Include [project.optional-dependencies] for dev deps.</action>
  <verify>python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))" succeeds</verify>
  <done>Valid pyproject.toml with all required dependencies listed</done>
</task>

<task type="auto">
  <name>Task 2: Create src package structure</name>
  <files>src/__init__.py, src/config/__init__.py, src/schemas/__init__.py</files>
  <action>Create directory structure:
```
src/
├── __init__.py          # Package root, version info
├── config/
│   └── __init__.py      # Config package (empty for now)
└── schemas/
    └── __init__.py      # Schemas package (empty for now)
```

In src/__init__.py, add:
```python
"""Jira Analyst Bot - Proactive requirements gathering via Slack."""
__version__ = "0.1.0"
```

Other __init__.py files can be empty or have docstrings only.</action>
  <verify>python -c "import sys; sys.path.insert(0, '.'); from src import __version__; print(__version__)"</verify>
  <done>Package structure created, importable from project root</done>
</task>

<task type="auto">
  <name>Task 3: Install dependencies and verify</name>
  <files>None (verification only)</files>
  <action>Run `pip install -e ".[dev]"` to install the package in editable mode with dev dependencies. This validates pyproject.toml is correct and all dependencies resolve.</action>
  <verify>pip install -e ".[dev]" completes without errors; python -c "import langgraph; import slack_bolt; import pydantic" succeeds</verify>
  <done>All dependencies installed and importable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pyproject.toml is valid TOML
- [ ] `pip install -e ".[dev]"` succeeds
- [ ] Core dependencies importable: langgraph, slack_bolt, pydantic, atlassian
- [ ] Package version accessible via `from src import __version__`
</verification>

<success_criteria>
- All tasks completed
- Project installable as editable package
- No import errors for core dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
