---
phase: 32-product-invariants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/slack/handlers/dispatch.py
  - src/slack/blocks/*.py
  - src/slack/formatters/*.py
autonomous: true
---

<objective>
Enforce SuperMode as sole UI contract (I1) — users ONLY see super-modes, never intent names.

Purpose: Users should think in 5 modes (BUILD, OPERATE, DECIDE, THINK, CHAT), not 13 intents. This simplifies user mental model while preserving internal routing granularity.

Output: All user-facing messages use super-mode labels. Intent names never leak to UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-product-invariants/REQUIREMENTS.md
@.planning/phases/32-product-invariants/32-CONTEXT.md

@src/schemas/intent.py
@src/slack/handlers/dispatch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SuperMode display helpers</name>
  <files>src/schemas/intent.py</files>
  <action>
1. Add display properties to SuperMode enum:
   - `label` property returning user-facing string ("Building...", "Operating...", etc.)
   - `emoji` property returning mode emoji (optional, for status lines)

2. Add `get_mode_status_line(super_mode, action_description)` function:
   - Returns formatted status line: `[BUILD] Creating ticket draft`
   - Format: `[{mode.label}] {description}`

3. Document in docstring:
   ```
   SuperMode = sole UI contract.
   Users see modes, never intents.
   Debug mode can show intents (developer tool).
   ```
  </action>
  <verify>python -c "from src.schemas.intent import SuperMode; print(SuperMode.BUILD.label)"</verify>
  <done>SuperMode has display properties and status line helper</done>
</task>

<task type="auto">
  <name>Task 2: Update dispatch to use super-mode in status messages</name>
  <files>src/slack/handlers/dispatch.py</files>
  <action>
1. Find all places where intent is used in user-facing messages
2. Replace with super_mode from IntentResult
3. Use `get_mode_status_line()` for status updates

Example changes:
- BEFORE: `f"Processing {intent.value} request..."`
- AFTER: `get_mode_status_line(result.super_mode, "processing request")`

4. Add comment at top of dispatch:
   ```python
   # INVARIANT I1: SuperMode = sole UI contract
   # All user-facing messages use super_mode, never intent.
   # Intent is internal routing detail only.
   ```

5. Ensure debug mode can still show intent (for developers):
   - Check for debug_mode flag
   - If debug: include intent in separate debug output
   - If not debug: only show super_mode
  </action>
  <verify>grep -c "super_mode" src/slack/handlers/dispatch.py</verify>
  <done>Dispatch uses super-mode for all user-facing status messages</done>
</task>

<task type="auto">
  <name>Task 3: Audit and update Slack blocks for super-mode</name>
  <files>src/slack/blocks/*.py</files>
  <action>
1. Search all block builders for intent references:
   ```bash
   grep -r "intent" src/slack/blocks/ | grep -v "__pycache__"
   ```

2. For each occurrence, determine if it's user-facing:
   - User-facing: Replace with super_mode equivalent
   - Internal/debug: Keep but add comment

3. Update any status/header blocks that show intent:
   - Use SuperMode.label instead of Intent.value
   - Use mode-appropriate emoji if applicable

4. Add INVARIANT comment to files with user-facing intent references:
   ```python
   # INVARIANT I1: Display super_mode to users, not intent
   ```
  </action>
  <verify>grep -rL "INVARIANT I1" src/slack/blocks/ | head -5</verify>
  <done>All Slack blocks use super-mode for user-facing content</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile src/schemas/intent.py` succeeds
- [ ] `python -m py_compile src/slack/handlers/dispatch.py` succeeds
- [ ] No intent names in user-facing messages (grep for leaked intents)
- [ ] SuperMode.label returns proper user-facing strings
- [ ] Debug mode still shows intent for developers
</verification>

<success_criteria>
- All tasks completed
- SuperMode has display properties (label, status line helper)
- Dispatch uses super-mode in all user-facing messages
- Slack blocks updated to use super-mode
- INVARIANT I1 documented in code
</success_criteria>

<output>
After completion, create `.planning/phases/32-product-invariants/32-01-SUMMARY.md`
</output>
