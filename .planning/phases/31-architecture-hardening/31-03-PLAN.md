---
phase: 31-architecture-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/slack/decision_manager.py
  - src/sync/decision_sync.py
  - src/slack/handlers/decision_handlers.py
autonomous: true
---

<objective>
Implement Slack failure tolerance and canonical message idempotency (R4, R5).

Purpose: Slack is presentation layer, not truth. If canonical message update fails, decision state and Jira sync must still proceed. Message updates must be idempotent and version-checked.

Output: Message failures logged but don't block approval/sync; retries are safe.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-architecture-hardening/REQUIREMENTS.md
@.planning/phases/31-architecture-hardening/31-CONTEXT.md

@src/slack/decision_manager.py
@src/sync/decision_sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version-checked update to DecisionManager</name>
  <files>src/slack/decision_manager.py</files>
  <action>
1. Add version parameter to update_canonical_message():
   async def update_canonical_message(
       self,
       decision: Decision,
       blocks: list[dict],
       expected_version: int | None = None,  # For idempotency check
   ) -> bool:

2. If expected_version is provided:
   - Compare with decision.version
   - If mismatch, log warning "Skipping stale update: expected v{expected_version}, decision at v{decision.version}"
   - Return True (stale update = success, no action needed)

3. Add docstring explaining idempotency:
   """Update canonical message with idempotency guarantee.

   IDEMPOTENT: Same version produces same blocks. Safe to retry.
   VERSION-CHECKED: Stale updates are skipped silently.
   FAILURE-SAFE: Update failure doesn't change decision state.
   """

4. Wrap the try/except to catch Slack errors but not raise them:
   - On SlackApiError: log error, return False
   - Never raise exceptions that would block caller
  </action>
  <verify>python -c "from src.slack.decision_manager import DecisionManager; import inspect; sig = inspect.signature(DecisionManager.update_canonical_message); print('expected_version' in str(sig))"</verify>
  <done>update_canonical_message is version-checked and idempotent</done>
</task>

<task type="auto">
  <name>Task 2: Ensure Slack failures don't block decision approval</name>
  <files>src/slack/handlers/decision_handlers.py</files>
  <action>
1. Find handle_decision_approve() function (or equivalent approval handler)

2. Ensure the flow is:
   a. DecisionStore.approve() → SUCCESS (state is truth) ← MUST succeed first
   b. DecisionSyncService.sync_decision() → proceeds regardless of Slack
   c. DecisionManager.update_canonical_message() → best effort
   d. If (c) fails: log warning, decision is still APPROVED, sync still happened

3. Add comments documenting the order:
   # CRITICAL ORDER:
   # 1. Database state update (truth)
   # 2. Jira sync (projection)
   # 3. Slack message (presentation - best effort)
   # Slack failure must NOT affect 1 or 2

4. If approval handler calls update_canonical_message before state update, reorder so state update is FIRST

5. Add logging when Slack update fails but approval/sync succeeded:
   logger.warning(
       "Slack message update failed, but decision is approved and synced",
       extra={"decision_id": decision.id, "version": decision.version}
   )
  </action>
  <verify>grep -A 20 "handle_decision_approve\|approve_decision" src/slack/handlers/decision_handlers.py | head -40</verify>
  <done>Approval flow: state first, Jira second, Slack third (best effort)</done>
</task>

<task type="auto">
  <name>Task 3: Add docstrings clarifying Slack as presentation layer</name>
  <files>src/slack/decision_manager.py, src/sync/decision_sync.py</files>
  <action>
1. In DecisionManager class docstring, add:
   """Manages canonical Slack messages for decisions.

   ARCHITECTURE:
   - Database is truth
   - Jira is projection
   - Slack is presentation (UI layer)

   FAILURE TOLERANCE:
   - Message update failures are logged, not raised
   - Caller can continue with approval/sync regardless of Slack
   - Retries are safe (idempotent updates)
   """

2. In DecisionSyncService.sync_decision() docstring, add:
   """Sync decision to all linked Jira tickets.

   NOTE: This method does NOT depend on Slack message state.
   Jira sync proceeds even if canonical message update failed.
   """

3. Ensure both files have the architecture comment:
   # Architecture: Database (truth) → Jira (projection) → Slack (presentation)
  </action>
  <verify>grep -c "Database is truth\|presentation\|Slack is" src/slack/decision_manager.py src/sync/decision_sync.py</verify>
  <done>Architecture documented: Database → Jira → Slack (presentation)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile src/slack/decision_manager.py` succeeds
- [ ] `python -m py_compile src/sync/decision_sync.py` succeeds
- [ ] update_canonical_message has expected_version parameter
- [ ] Approval handler has correct order: state → jira → slack
- [ ] Architecture comments present in both files
</verification>

<success_criteria>
- All tasks completed
- Slack message failures don't block state updates
- update_canonical_message is idempotent with version check
- Architecture documented: Database (truth) → Jira (projection) → Slack (presentation)
</success_criteria>

<output>
After completion, create `.planning/phases/31-architecture-hardening/31-03-SUMMARY.md`
</output>
