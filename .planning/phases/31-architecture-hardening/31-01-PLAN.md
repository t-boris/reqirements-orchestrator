---
phase: 31-architecture-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/intent.py
  - src/graph/intent.py
  - docs/HOW_THE_BOT_THINKS.md
autonomous: true
---

<objective>
Add super_mode field to IntentResult for user-facing simplicity.

Purpose: Users should think in 5 modes (BUILD, OPERATE, DECIDE, THINK, CHAT), not 13 intents. This is presentation layer only — internal routing still uses fine-grained intents.

Output: IntentResult has super_mode field populated on every classification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-architecture-hardening/REQUIREMENTS.md
@.planning/phases/31-architecture-hardening/31-CONTEXT.md

@src/schemas/intent.py
@src/graph/intent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SuperMode enum and field to IntentResult</name>
  <files>src/schemas/intent.py</files>
  <action>
1. Add SuperMode enum with 5 values:
   - BUILD: "I'm building something"
   - OPERATE: "I'm managing Jira"
   - DECIDE: "I'm recording a decision"
   - THINK: "Help me think"
   - CHAT: "Just talking"

2. Add super_mode field to IntentResult (Optional[SuperMode])

3. Add INTENT_TO_SUPER_MODE mapping dict:
   - BUILD: WORKITEM_CREATE, DRAFT_REFINE, DRAFT_TRANSFORM
   - OPERATE: JIRA_COMMAND, CHANGE_REQUEST, SYNC_REQUEST, TICKET_ACTION
   - DECIDE: DECISION
   - THINK: REVIEW, JIRA_SEARCH
   - CHAT: DISCUSSION, META, AMBIGUOUS

4. Add helper function `get_super_mode(intent: Intent) -> SuperMode` that looks up the mapping
  </action>
  <verify>python -c "from src.schemas.intent import SuperMode, IntentResult, get_super_mode, Intent; print(get_super_mode(Intent.WORKITEM_CREATE))"</verify>
  <done>SuperMode enum exists, IntentResult has super_mode field, mapping function works</done>
</task>

<task type="auto">
  <name>Task 2: Populate super_mode in intent classification</name>
  <files>src/graph/intent.py</files>
  <action>
1. Import SuperMode and get_super_mode from src.schemas.intent

2. In classify_intent() function, after determining intent, add:
   super_mode = get_super_mode(result.intent)
   result.super_mode = super_mode

3. Also update the IntentResult constructor in _llm_classify() to populate super_mode

4. For the pattern-matched DECISION path, add super_mode=SuperMode.DECIDE to the IntentResult
  </action>
  <verify>python -c "import asyncio; from src.graph.intent import classify_intent; r = asyncio.run(classify_intent('create a ticket')); print(f'intent={r.intent}, super_mode={r.super_mode}')"</verify>
  <done>Every IntentResult from classify_intent has super_mode populated</done>
</task>

<task type="auto">
  <name>Task 3: Update HOW_THE_BOT_THINKS.md with super-modes</name>
  <files>docs/HOW_THE_BOT_THINKS.md</files>
  <action>
1. Add a new section "## 2. User Modes" after the Intent Classification section

2. Explain the 5 super-modes as the user-facing model:
   | Mode | User Perception | Internal Intents |
   |------|-----------------|------------------|
   | BUILD | "I'm building something" | WORKITEM_CREATE, DRAFT_REFINE, DRAFT_TRANSFORM |
   | OPERATE | "I'm managing Jira" | JIRA_COMMAND, CHANGE_REQUEST, SYNC_REQUEST, TICKET_ACTION |
   | DECIDE | "I'm recording a decision" | DECISION |
   | THINK | "Help me think" | REVIEW, JIRA_SEARCH |
   | CHAT | "Just talking" | DISCUSSION, META, AMBIGUOUS |

3. Add note: "13 intents exist for fine-grained routing. 5 modes exist for user communication."

4. Update the summary section to mention super-modes
  </action>
  <verify>grep -c "SuperMode\|super-mode\|BUILD\|OPERATE\|DECIDE\|THINK\|CHAT" docs/HOW_THE_BOT_THINKS.md</verify>
  <done>Documentation explains 5 modes for users, 13 intents for routing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile src/schemas/intent.py` succeeds
- [ ] `python -m py_compile src/graph/intent.py` succeeds
- [ ] SuperMode enum has 5 values
- [ ] IntentResult.super_mode is populated on classification
- [ ] docs/HOW_THE_BOT_THINKS.md has super-mode section
</verification>

<success_criteria>
- All tasks completed
- SuperMode enum with BUILD, OPERATE, DECIDE, THINK, CHAT
- Every IntentResult has super_mode populated
- Documentation presents 5 modes (not 13 intents) to users
</success_criteria>

<output>
After completion, create `.planning/phases/31-architecture-hardening/31-01-SUMMARY.md`
</output>
