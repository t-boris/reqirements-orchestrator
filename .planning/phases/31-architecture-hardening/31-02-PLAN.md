---
phase: 31-architecture-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jira/managed_sections.py
  - tests/test_managed_sections.py
autonomous: true
---

<objective>
Enforce MANAGED_SECTION_ONLY invariant as a hard rule with validation.

Purpose: Decision projection must NEVER modify user content outside the managed section. This elevates a design note to an enforced invariant with tests proving correctness.

Output: Managed section updates are validated, reject if boundaries unclear.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-architecture-hardening/REQUIREMENTS.md
@.planning/phases/31-architecture-hardening/31-CONTEXT.md

@src/jira/managed_sections.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validation to managed section updates</name>
  <files>src/jira/managed_sections.py</files>
  <action>
1. Add ManagedSectionError exception class at top of file:
   class ManagedSectionError(Exception):
       """Raised when managed section operation would violate invariants."""
       pass

2. Add validate_section_boundaries() function that checks:
   - If section exists: start marker followed by end marker (not reversed)
   - No nested section markers
   - If updating: old content is fully contained within section boundaries
   Returns True if valid, raises ManagedSectionError with message if not

3. Update update_description_with_managed_section() to:
   - Call validate_section_boundaries() before modifying
   - Add docstring stating the invariant: "INVARIANT: Only touches content between section markers. User content above/below is NEVER modified."
   - If validation fails, raise ManagedSectionError (don't silently proceed)

4. Add verify_user_content_preserved() function that takes (before, after) descriptions and:
   - Extracts user content (everything outside managed section)
   - Returns True if user content is identical
   - Can be used in tests for double-checking
  </action>
  <verify>python -c "from src.jira.managed_sections import ManagedSectionError, validate_section_boundaries; print('ManagedSectionError exists')"</verify>
  <done>ManagedSectionError exists, validation rejects malformed sections</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive tests for managed section invariants</name>
  <files>tests/test_managed_sections.py</files>
  <action>
1. Create new test file tests/test_managed_sections.py

2. Add test cases:
   - test_update_preserves_user_content_above: User content before section is preserved
   - test_update_preserves_user_content_below: User content after section is preserved
   - test_update_preserves_mixed_content: Content both above and below preserved
   - test_append_when_no_section: Appends section without touching existing content
   - test_rejects_nested_markers: Raises error if section markers are nested
   - test_rejects_reversed_markers: Raises error if end marker comes before start
   - test_fully_reversible: Removing managed section restores original (minus section)

3. Each test should:
   - Set up a description with user content
   - Call update_description_with_managed_section
   - Use verify_user_content_preserved() to confirm invariant
   - Assert specific behavior

4. Add test docstrings explaining what invariant each test proves
  </action>
  <verify>python -m pytest tests/test_managed_sections.py -v --tb=short 2>/dev/null || python -m pytest tests/test_managed_sections.py -v</verify>
  <done>Tests prove: user content never modified, malformed sections rejected, operation is reversible</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile src/jira/managed_sections.py` succeeds
- [ ] All tests in tests/test_managed_sections.py pass
- [ ] ManagedSectionError is raised for malformed sections
- [ ] User content is provably preserved in all update scenarios
</verification>

<success_criteria>
- All tasks completed
- MANAGED_SECTION_ONLY invariant enforced in code
- Tests prove user content is never modified
- Tests prove malformed sections are rejected
- Invariant is documented in code
</success_criteria>

<output>
After completion, create `.planning/phases/31-architecture-hardening/31-02-SUMMARY.md`
</output>
