---
phase: 31-architecture-hardening
plan: 04
type: execute
wave: 2
depends_on: ["31-01", "31-02", "31-03"]
files_modified:
  - docs/HOW_THE_BOT_THINKS.md
  - docs/architecture.md
autonomous: true
---

<objective>
Finalize documentation updates for Phase 31 (R6, R8).

Purpose: Clarify commit log vs state separation and sync semantics. Document all architectural hardening decisions from this phase.

Output: Updated docs reflecting Phase 31 changes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-architecture-hardening/REQUIREMENTS.md
@.planning/phases/31-architecture-hardening/31-CONTEXT.md

@docs/HOW_THE_BOT_THINKS.md
@docs/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Commit Log vs State separation (R6)</name>
  <files>docs/HOW_THE_BOT_THINKS.md</files>
  <action>
1. Add section "## 6. Commit Log vs State" after the existing sections

2. Explain the distinction:
   | Concept | Nature | Mutability | Purpose |
   |---------|--------|------------|---------|
   | Canonical Message | Current state (HEAD) | Mutable (updated in place) | What IS true now |
   | Commit Log | Historical record | Append-only (never edited) | What BECAME true |

3. Add rules:
   - Commit log entries are immutable after creation
   - Canonical messages are updated to reflect current version
   - Never merge these concepts in code or docs

4. Add example flow:
   Decision approved → Commit log entry created (immutable) → Canonical message updated (mutable)
  </action>
  <verify>grep -c "Commit Log vs State\|append-only\|immutable" docs/HOW_THE_BOT_THINKS.md</verify>
  <done>Commit log vs state distinction documented</done>
</task>

<task type="auto">
  <name>Task 2: Document Sync Semantics (R8)</name>
  <files>docs/HOW_THE_BOT_THINKS.md</files>
  <action>
1. Add section "## 7. Sync Semantics" or update existing preflight section

2. Document the two sync mechanisms:
   | Operation | Blocking? | Purpose |
   |-----------|-----------|---------|
   | Preflight | Yes | Guard before any Jira write |
   | /maro sync | No | Informational reconciliation |

3. Explain:
   - Preflight is called before create/update operations
   - Preflight result determines if operation proceeds
   - `/maro sync` reports drift but doesn't block anything
   - Both use same conflict classification (IDEMPOTENT, SAFE_DRIFT, REAL_CONFLICT, STRUCTURAL)

4. Add note: "Preflight = write guard. Sync = read-only diagnostic."
  </action>
  <verify>grep -c "Sync Semantics\|Preflight\|blocking\|informational" docs/HOW_THE_BOT_THINKS.md</verify>
  <done>Sync semantics documented: preflight = blocking, sync = informational</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 31 section to architecture.md</name>
  <files>docs/architecture.md</files>
  <action>
1. Add "## Phase 31: Architecture Hardening" section

2. Document the hardening changes:
   - **Super-modes**: 13 intents grouped into 5 user-facing modes (BUILD, OPERATE, DECIDE, THINK, CHAT)
   - **MANAGED_SECTION_ONLY invariant**: Decision projection only touches managed section
   - **Slack as UI**: Message failures don't block state or Jira sync
   - **Canonical message idempotency**: Version-checked, failure-safe, retry-safe
   - **Commit log vs state**: Append-only log vs mutable messages

3. Add architecture diagram (text-based):
   ```
   User Input
       ↓
   Intent Router → SuperMode (user) + Intent (routing)
       ↓
   Decision Flow
       ↓
   DecisionStore.approve() ← STATE (truth)
       ↓
   DecisionSyncService → JIRA (projection)
       ↓
   DecisionManager → SLACK (presentation, best effort)
   ```

4. Add "Mantra: Feel like a product, not an OS kernel."
  </action>
  <verify>grep -c "Phase 31\|Architecture Hardening\|super-mode\|MANAGED_SECTION_ONLY" docs/architecture.md</verify>
  <done>Phase 31 architectural changes documented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docs/HOW_THE_BOT_THINKS.md has Commit Log vs State section
- [ ] docs/HOW_THE_BOT_THINKS.md has Sync Semantics section
- [ ] docs/architecture.md has Phase 31 section
- [ ] All Phase 31 decisions documented
</verification>

<success_criteria>
- All tasks completed
- Commit log vs state distinction documented
- Sync semantics (preflight vs sync) documented
- Phase 31 architectural changes in architecture.md
- "Feel like a product, not an OS kernel" mantra present
</success_criteria>

<output>
After completion, create `.planning/phases/31-architecture-hardening/31-04-SUMMARY.md`
</output>
