---
phase: 28-structured-draft-evolution
plan: 01
subsystem: schemas
tags: [pydantic, state-machine, typed-draft, lifecycle]

# Dependency graph
requires:
  - phase: 27-multi-user-support
    provides: TicketDraft with attribution tracking
provides:
  - StructuredDraft typed design object
  - DraftKind, DraftScope, DraftLifecycle enums
  - DraftItem, DraftItemStatus, DraftChange models
  - Bidirectional migration (TicketDraft <-> StructuredDraft)
  - AgentState.structured_draft field
affects: [28-structured-draft-evolution, draft-transform, intent-routing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - State machine pattern for draft lifecycle
    - Typed enums for draft kind/scope/status
    - Change log for audit trail

key-files:
  created:
    - src/schemas/structured_draft.py
  modified:
    - src/schemas/draft.py
    - src/schemas/state.py
    - src/schemas/__init__.py

key-decisions:
  - "Draft lifecycle uses explicit state machine with _VALID_TRANSITIONS"
  - "DraftItem carries compatibility fields (problem, proposed_solution, acceptance_criteria) from TicketDraft"
  - "Bidirectional conversion enables gradual migration"
  - "structured_draft field Optional in AgentState for dual-draft transition period"

patterns-established:
  - "State machine validation via can_transition_to() method"
  - "Change audit trail via DraftChange log"
  - "Migration helpers as class methods"

issues-created: []

# Metrics
duration: 4min
completed: 2026-01-23
---

# Phase 28 Plan 01: StructuredDraft Schema Summary

**Typed design object schema with lifecycle states, structural enums, and bidirectional migration from TicketDraft**

## Performance

- **Duration:** 4 min
- **Started:** 2026-01-23T18:26:01Z
- **Completed:** 2026-01-23T18:29:52Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Created StructuredDraft schema with 7 models: DraftKind, DraftScope, DraftItemStatus, DraftLifecycle, DraftItem, DraftChange, StructuredDraft
- Implemented state machine validation via can_transition_to() method with explicit transition rules
- Added bidirectional migration helpers: StructuredDraft.from_ticket_draft() and to_ticket_draft()
- Integrated structured_draft field into AgentState TypedDict
- Exported all new types from src.schemas package

## Task Commits

Each task was committed atomically:

1. **Task 1: Create StructuredDraft schema with all type definitions** - `66551ba` (feat)
2. **Task 2: Add migration helpers and TicketDraft compatibility** - `5057306` (feat)
3. **Task 3: Integrate StructuredDraft into AgentState** - `a63b904` (feat)

## Files Created/Modified

- `src/schemas/structured_draft.py` - New schema file with StructuredDraft and related models
- `src/schemas/draft.py` - Added to_structured() migration helper to TicketDraft
- `src/schemas/state.py` - Added structured_draft field to AgentState
- `src/schemas/__init__.py` - Exported all Phase 28 schema types

## Decisions Made

1. **State machine with explicit transitions** - Used _VALID_TRANSITIONS dict to enforce lifecycle rules, making invalid transitions detectable at runtime
2. **DraftItem carries TicketDraft fields** - Included problem, proposed_solution, acceptance_criteria for backward compatibility during migration
3. **Bidirectional conversion** - Enables gradual migration with ability to fall back to TicketDraft for legacy code paths
4. **Optional structured_draft** - AgentState keeps both draft (legacy) and structured_draft (new) during transition period

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

- StructuredDraft schema complete with all type definitions
- Ready for Plan 02: Lifecycle Transitions (implement transformation mutations)
- AgentState has structured_draft field for Phase 28 nodes to populate

---
*Phase: 28-structured-draft-evolution*
*Completed: 2026-01-23*
