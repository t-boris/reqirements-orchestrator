---
phase: 28-structured-draft-evolution
plan: 06
subsystem: ui
tags: [slack, blocks, structured-draft, visualization, approval, versioning]

# Dependency graph
requires:
  - phase: 28.3
    provides: Mutation methods on StructuredDraft
  - phase: 28.4
    provides: Lifecycle state machine, transition_lifecycle
provides:
  - Structure visualization blocks (build_structure_blocks)
  - Version-bound approval buttons (R9)
  - Stale structure detection with ephemeral messages (T4)
  - Post-transform structure display (R8)
  - Edit structure prompt handler
affects: [28.7+, jira-creation-flow, draft-approval-flow]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Version-bound button payloads with JSON
    - Ephemeral messages for stale approval feedback

key-files:
  created:
    - src/slack/blocks/draft_structure.py
  modified:
    - src/slack/handlers/draft.py
    - src/slack/handlers/dispatch.py
    - src/slack/handlers/__init__.py
    - src/slack/router.py

key-decisions:
  - "Text-based blocks for structure (not complex visual diagrams) - simpler, mobile-friendly"
  - "Stories indented under epics with '└─' prefix for hierarchy"
  - "Ephemeral messages for stale approvals (private) rather than in-thread (public)"
  - "Edit button prompts user to describe changes, routes to DRAFT_TRANSFORM intent"

patterns-established:
  - "Version binding: JSON payloads with {draft_id, version} in action buttons"
  - "Stale detection: Compare button version vs current draft version"

issues-created: []

# Metrics
duration: 4min
completed: 2026-01-23
---

# Phase 28 Plan 06: Structure Feedback UI Summary

**Structure visualization blocks with version-bound approval buttons, stale detection, and post-transform display for StructuredDraft (R8, R9)**

## Performance

- **Duration:** 4 min
- **Started:** 2026-01-23T19:50:18Z
- **Completed:** 2026-01-23T19:54:02Z
- **Tasks:** 4
- **Files modified:** 5

## Accomplishments

- Created `build_structure_blocks()` function to visualize StructuredDraft hierarchy
- Version-bound approval buttons detect stale structures and show ephemeral "outdated" message (T4)
- Structure visualization posted automatically after each transform_applied action (R8)
- Edit structure button prompts user with transform command examples

## Task Commits

Each task was committed atomically:

1. **Task 1: Create structure visualization blocks module** - `5a409bd` (feat)
2. **Task 2: Add stale structure approval detection** - `5b4c0da` (feat)
3. **Task 3: Post structure after transform_applied** - `55f0a37` (feat)
4. **Task 4: Add edit_structure handler** - `10cd77e` (feat)

## Files Created/Modified

- `src/slack/blocks/draft_structure.py` - New module for StructuredDraft visualization blocks
- `src/slack/handlers/draft.py` - Added handle_approve_structure and handle_edit_structure handlers
- `src/slack/handlers/dispatch.py` - Added transform_applied action handler with structure display
- `src/slack/handlers/__init__.py` - Exported new handlers
- `src/slack/router.py` - Registered approve_structure and edit_structure action handlers

## Decisions Made

1. **Text-based structure visualization** - Uses mrkdwn blocks rather than complex visual diagrams; simpler, works on mobile
2. **Hierarchy display** - Stories under epics shown with "└─" indent prefix
3. **Stale approval feedback** - Uses ephemeral messages (private to user) rather than public thread messages
4. **Edit prompt flow** - Edit button posts prompt text with example commands; user response routes to DRAFT_TRANSFORM intent

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

- Structure feedback UI complete (R8, R9)
- DoD Test T4 (stale button detection) now passes
- Ready for next Phase 28 plan or phase completion

---
*Phase: 28-structured-draft-evolution*
*Completed: 2026-01-23*
