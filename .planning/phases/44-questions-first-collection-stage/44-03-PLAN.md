---
phase: 44-questions-first-collection-stage
plan: 03
type: execute
wave: 2
depends_on: ["44-01", "44-02"]
files_modified: [src/graph/intent_router.py, src/graph/intent_gates.py]
autonomous: true
---

<objective>
Integrate triage gate into the intent classification pipeline, inserting it before Stage 1 mode classification.

Purpose: Add questions-first stage that blocks classification when context is too incomplete.
Output: Modified intent_router.py with triage gate integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-questions-first-collection-stage/44-CONTEXT.md

Key files:
@src/graph/intent_router.py - Main router to modify
@src/graph/intent_gates.py - Existing gate patterns

From 44-01-SUMMARY.md: TriageContext, check_triage_needed()
From 44-02-SUMMARY.md: QuestionEngine.generate_triage_question()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TRIAGE gate result to intent_gates.py</name>
  <files>src/graph/intent_gates.py</files>
  <action>
Extend GateResult enum with TRIAGE:

1. Add to GateResult enum:
   TRIAGE = "triage"  # Needs triage questions before classification

2. Add triage fields to PreGateOutput:
   # For TRIAGE: context too incomplete
   triage_context: Optional["TriageContext"] = None
   triage_question: Optional["QuestionTask"] = None

3. Import TriageContext, TriageGap at top (TYPE_CHECKING block)

This extends existing pattern without breaking it.
  </action>
  <verify>python -c "from src.graph.intent_gates import GateResult; print(GateResult.TRIAGE.value)"</verify>
  <done>GateResult.TRIAGE added, PreGateOutput has triage_context field</done>
</task>

<task type="auto">
  <name>Task 2: Add triage check to run_pre_gates</name>
  <files>src/graph/intent_gates.py</files>
  <action>
Add triage gate check to run_pre_gates() as Gate 0 (before terminal handling):

1. Import check_triage_needed from src.graph.triage_gate

2. Add new gate function check_triage_gate(state, message) -> Optional[PreGateOutput]:
   - Call check_triage_needed(state, message)
   - If triage_context.fast_path is True, return None (proceed)
   - If gaps exist and completeness_score < threshold:
     return PreGateOutput(
         result=GateResult.TRIAGE,
         triage_context=triage_context,
     )

3. In run_pre_gates(), add Gate 0 before Gate 1:
   # Gate 0: Triage (completeness check)
   result = check_triage_gate(state, message)
   if result:
       logger.info(f"Pre-gate: triage needed - completeness={result.triage_context.completeness_score}")
       return result

   # Gate 1: Terminal handling (existing)
   ...

Key: Triage gate should NOT trigger if:
- is_command is True (slash commands bypass triage)
- State already has triage answers (prevent loops)
- Message is short greeting (<5 words, no signals)

Add state.get("triage_answers") check to prevent re-asking.
  </action>
  <verify>python -c "from src.graph.intent_gates import run_pre_gates, GateResult; print('OK')"</verify>
  <done>run_pre_gates() includes triage check before terminal handling</done>
</task>

<task type="auto">
  <name>Task 3: Handle TRIAGE result in intent_router.py</name>
  <files>src/graph/intent_router.py</files>
  <action>
Modify route_intent() to handle GateResult.TRIAGE:

1. Import TriageContext, QuestionTask

2. Add triage handling after gate_output check:
   # Handle triage (incomplete context)
   if gate_output.result == GateResult.TRIAGE:
       logger.info(f"Triage needed: completeness={gate_output.triage_context.completeness_score}")
       return RouterResult(
           envelope=_create_triage_envelope(gate_output),
           stage0_gate=gate_output,
           bypassed=True,
           bypass_reason="Triage questions needed",
       )

3. Add _create_triage_envelope(gate: PreGateOutput) -> IntentEnvelope:
   """Create envelope requesting triage questions.

   Returns ambiguous envelope with kind=AMBIGUOUS, but with
   special triage_context that dispatch will handle.
   """
   # Create "ambiguous" envelope that asks for clarification
   return IntentEnvelope(
       kind=EnvelopeKind.AMBIGUOUS,
       mode=SuperMode.CHAT,  # Triage is pre-mode
       confidence=gate.triage_context.completeness_score,
       reason="Context incomplete, asking clarifying questions",
       alternatives=[],  # Will be filled by dispatch
       triage_context=gate.triage_context,
   )

4. Add triage_context field to RouterResult:
   triage_context: Optional[TriageContext] = None

5. In RouterResult assignment for triage case, include triage_context.

Note: We use EnvelopeKind.AMBIGUOUS because the dispatch handler already knows how to post questions for ambiguous intents. The triage_context field signals that these are triage questions (not intent alternatives).
  </action>
  <verify>python -c "from src.graph.intent_router import route_intent; print('OK')"</verify>
  <done>route_intent() handles GateResult.TRIAGE and returns appropriate envelope</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] GateResult.TRIAGE exists
- [ ] PreGateOutput has triage_context field
- [ ] run_pre_gates() returns TRIAGE result when context incomplete
- [ ] route_intent() handles TRIAGE and returns RouterResult with triage_context
- [ ] Commands (is_command=True) bypass triage
- [ ] State with triage_answers skips triage (no re-asking)
</verification>

<success_criteria>
- All tasks completed
- Triage gate integrates cleanly with existing gate pattern
- No breaking changes to existing bypass/constrain/guard flow
- Triage only triggers when completeness below threshold
- Fast path skips triage for complete contexts
</success_criteria>

<output>
After completion, create `.planning/phases/44-questions-first-collection-stage/44-03-SUMMARY.md`
</output>
