---
phase: 44-questions-first-collection-stage
plan: 05
type: execute
wave: 3
depends_on: ["44-03", "44-04"]
files_modified: [src/slack/handlers/triage.py, src/slack/blocks/triage.py]
autonomous: true
---

<objective>
Create Slack UI handlers for triage questions - posting questions and handling button clicks.

Purpose: Display triage questions with buttons and collect user answers.
Output: Triage button handlers and question block builders.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-questions-first-collection-stage/44-CONTEXT.md

Existing handlers to follow:
@src/slack/handlers/question.py - Question button handlers
@src/slack/blocks/question.py - Question block builders
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create triage block builder</name>
  <files>src/slack/blocks/triage.py</files>
  <action>
Create Slack block builder for triage questions:

1. build_triage_question_blocks(question: QuestionTask, thread_ts: str) -> list[dict]:
   """Build Slack blocks for a triage question.

   Args:
       question: QuestionTask from TriageProvider
       thread_ts: Thread timestamp for button value encoding

   Returns:
       List of Slack blocks with question and buttons
   """
   Format:
   - Section block with question text (friendly intro)
   - Divider
   - Actions block with buttons for options
   - Context block with hint text

2. Button value encoding (JSON):
   {
       "question_id": question.question_id,
       "target_field": question.target_field,
       "value": option.value,
       "thread_ts": thread_ts
   }

3. Action ID format: "triage_answer_{target_field}"
   e.g., "triage_answer_mode_hint"

4. Visual design:
   - Intro: "Before I get started, I'd like to understand what you need:"
   - Question text in bold
   - Primary button for recommended option (if is_recommended)
   - Context: "_Click a button or just reply with more details._"

5. For ASK_USER questions (no buttons):
   - Section with question text
   - Context: "_Just reply in the thread with your answer._"

Follow build_question_blocks() pattern but simpler (no plan_id/version needed).
  </action>
  <verify>python -c "from src.slack.blocks.triage import build_triage_question_blocks; print('OK')"</verify>
  <done>build_triage_question_blocks() returns valid Slack blocks</done>
</task>

<task type="auto">
  <name>Task 2: Create triage button handler</name>
  <files>src/slack/handlers/triage.py</files>
  <action>
Create handler for triage button clicks:

1. async def handle_triage_answer(
       ack: Ack,
       body: dict,
       client: WebClient,
       logger: logging.Logger,
   ) -> None:
   """Handle triage question button click.

   Flow:
   1. Extract answer from button value
   2. Save to TriageStore
   3. Check if more triage questions needed
   4. If complete: re-invoke intent router with enriched context
   5. If more needed: post next question
   """

2. Parse button value (JSON):
   - question_id
   - target_field (e.g., "triage.mode_hint" → "mode_hint")
   - value (e.g., "build")
   - thread_ts

3. Save to TriageStore:
   - Call store.update_field(channel_id, thread_ts, field, value)

4. Check completion:
   - Load current triage answers
   - Run check_triage_needed() with updated state
   - If fast_path now True → trigger_intent_reclassification()
   - If gaps remain → post next triage question

5. trigger_intent_reclassification(channel_id, thread_ts, triage_answers):
   """Re-invoke the message handler with enriched context.

   This will re-run the graph with triage_answers in state,
   which should now pass the triage gate and proceed to classification.
   """
   # Post acknowledgment message
   client.chat_postMessage(
       channel=channel_id,
       thread_ts=thread_ts,
       text="Thanks! Let me process your request now...",
   )
   # Re-invoke message processing (call back to dispatch or graph)

6. Register handler with action_id pattern: "triage_answer_*"

Key: After collecting enough answers, we re-run the graph with triage_answers populated. The triage gate will see answers exist and pass through.
  </action>
  <verify>python -c "from src.slack.handlers.triage import handle_triage_answer; print('OK')"</verify>
  <done>handle_triage_answer() saves answers and triggers re-classification when complete</done>
</task>

<task type="auto">
  <name>Task 3: Register triage handlers in app</name>
  <files>src/slack/handlers/__init__.py</files>
  <action>
Register triage handlers:

1. Import handle_triage_answer from triage module

2. Add registration function:
   def register_triage_handlers(app: App) -> None:
       """Register triage question handlers."""
       app.action(re.compile(r"triage_answer_.*"))(handle_triage_answer)

3. Call register_triage_handlers(app) in the main registration flow

Follow pattern of register_question_handlers().
  </action>
  <verify>python -c "from src.slack.handlers import register_triage_handlers; print('OK')"</verify>
  <done>register_triage_handlers() exported and registers action handlers</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] build_triage_question_blocks() returns valid Slack block kit JSON
- [ ] Buttons have proper action_id and value encoding
- [ ] handle_triage_answer() parses button value correctly
- [ ] TriageStore.update_field() called on button click
- [ ] Re-classification triggered when triage complete
- [ ] Handler registered with pattern "triage_answer_*"
</verification>

<success_criteria>
- All tasks completed
- Blocks follow existing question block patterns
- Handler follows existing button handler patterns
- JSON value encoding is robust (handles special chars)
- Re-classification flow works without infinite loops
</success_criteria>

<output>
After completion, create `.planning/phases/44-questions-first-collection-stage/44-05-SUMMARY.md`
</output>
