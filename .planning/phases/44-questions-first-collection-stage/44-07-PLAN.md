---
phase: 44-questions-first-collection-stage
plan: 07
type: execute
wave: 4
depends_on: ["44-05", "44-06"]
files_modified: [src/slack/handlers/dispatch/core.py, src/graph/graph.py]
autonomous: true
---

<objective>
Integrate triage flow into dispatch and graph for end-to-end questions-first routing.

Purpose: Connect triage gate results to Slack message posting and graph routing.
Output: Complete end-to-end triage flow from message → questions → re-classification → routing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-questions-first-collection-stage/44-CONTEXT.md

Key files:
@src/slack/handlers/dispatch/core.py - Main dispatch handler
@src/graph/graph.py - Graph structure and routing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add triage routing to graph</name>
  <files>src/graph/graph.py</files>
  <action>
Add triage handling to the graph routing:

1. Import triage types:
   from src.schemas.triage import TriageContext
   from src.graph.intent_gates import GateResult

2. Modify route_after_intent() to check for triage:
   def route_after_intent(state: dict) -> str:
       """Route based on envelope kind."""
       envelope = state.get("envelope")
       stage0_gate = state.get("stage0_gate")

       # Handle triage (incomplete context)
       if stage0_gate and stage0_gate.result == GateResult.TRIAGE:
           return "triage_questions"

       # Existing routing...
       if not envelope:
           return "error_handler"
       ...

3. Add triage_questions node to graph:
   async def triage_questions_node(state: dict) -> dict:
       """Generate and return triage question for posting.

       Does NOT post to Slack - dispatch handler does that.
       """
       from src.questions.triage_provider import TriageProvider

       gate_output = state.get("stage0_gate")
       if not gate_output or not gate_output.triage_context:
           return {"decision_result": {"action": "error", "message": "No triage context"}}

       triage_context = gate_output.triage_context
       provider = TriageProvider()

       question = provider.get_next_question(
           triage_context.gaps,
           {"message": state.get("event", {}).get("text", "")},
       )

       return {
           "decision_result": {
               "action": "triage_question",
               "question": question,
               "triage_context": triage_context,
           }
       }

4. Add node to graph builder:
   graph.add_node("triage_questions", triage_questions_node)

5. Add edge from intent_router to triage_questions:
   graph.add_conditional_edges(
       "intent_router",
       route_after_intent,
       {
           "triage_questions": "triage_questions",
           # ... existing routes
       }
   )

6. Triage questions node routes to END (dispatch posts the question).
  </action>
  <verify>python -c "from src.graph.graph import graph; print('OK')"</verify>
  <done>Graph has triage_questions node and routes to it when triage needed</done>
</task>

<task type="auto">
  <name>Task 2: Handle triage_question action in dispatch</name>
  <files>src/slack/handlers/dispatch/core.py</files>
  <action>
Add handler for triage_question action in dispatch:

1. Import triage block builder:
   from src.slack.blocks.triage import build_triage_question_blocks

2. In _dispatch_decision_result() or equivalent dispatcher:
   if action == "triage_question":
       await _handle_triage_question(
           result=result,
           identity=identity,
           client=client,
       )
       return

3. Add _handle_triage_question():
   async def _handle_triage_question(
       result: dict,
       identity: SessionIdentity,
       client: WebClient,
   ) -> None:
       """Post triage question to Slack.

       Args:
           result: Decision result with question and triage_context
           identity: Session identity (channel, thread)
           client: Slack client
       """
       question = result.get("question")
       if not question:
           logger.warning("Triage action but no question to post")
           return

       # Build blocks
       blocks = build_triage_question_blocks(
           question=question,
           thread_ts=identity.thread_ts or "",
       )

       # Post to thread
       client.chat_postMessage(
           channel=identity.channel_id,
           thread_ts=identity.thread_ts,
           blocks=blocks,
           text=question.question_text,  # Fallback
       )

       logger.info(
           f"Posted triage question: {question.question_id}",
           extra={
               "question_type": question.question_type.value,
               "target_field": question.target_field,
           }
       )

4. Import at top: from src.schemas.question import QuestionTask
  </action>
  <verify>python -c "from src.slack.handlers.dispatch.core import _handle_triage_question; print('OK')" 2>/dev/null || echo "Function is private, check file manually"</verify>
  <done>Dispatch handles triage_question action and posts question to Slack</done>
</task>

<task type="auto">
  <name>Task 3: Add fast-path acknowledgment</name>
  <files>src/slack/handlers/dispatch/core.py</files>
  <action>
When triage passes (fast path), optionally log or acknowledge:

1. In dispatch, if envelope was classified without triage:
   # This is informational - no need to post anything to user
   # Just log that fast path was taken
   if not state.get("triage_triggered"):
       logger.debug("Fast path: context complete, no triage needed")

2. When triage IS triggered, the triage_questions node returns action="triage_question"
   and dispatch posts the question. No separate acknowledgment needed.

3. Add metric/logging for triage usage:
   - Count of messages triggering triage vs fast path
   - Average completeness_score when triage triggered
   - Most common gaps (for later optimization)

This is observability only - no user-facing changes for fast path.
  </action>
  <verify>grep -l "triage" src/slack/handlers/dispatch/core.py</verify>
  <done>Dispatch logs triage usage for observability</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Graph routes to triage_questions node when GateResult.TRIAGE
- [ ] triage_questions_node returns decision_result with action="triage_question"
- [ ] Dispatch handles triage_question action
- [ ] Triage question blocks posted to correct thread
- [ ] Fast path (no triage) continues to work unchanged
- [ ] Logging captures triage metrics
</verification>

<success_criteria>
- All tasks completed
- End-to-end flow: message → triage gate → questions → answer → re-classify → route
- No breaking changes to existing non-triage paths
- Questions appear in thread with buttons
- Fast path adds no latency (triage check is O(1) deterministic)
</success_criteria>

<output>
After completion, create `.planning/phases/44-questions-first-collection-stage/44-07-SUMMARY.md`
</output>
