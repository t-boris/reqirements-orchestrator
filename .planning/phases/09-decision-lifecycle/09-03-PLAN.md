---
phase: 09-decision-lifecycle
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/modes/record.py
  - src/slack/handlers/actions.py
autonomous: true
---

<objective>
Add decision amendment detection in RECORD mode and wire up the amend flow in Slack handlers.

Purpose: When a user invokes RECORD mode in a thread that already has a recorded decision, the bot should detect this and offer to amend the existing decision rather than blindly creating a duplicate. The Slack action handlers need to call amend_decision() and update the pinned ADR message.

Output: RECORD mode detects existing decisions in thread and offers amend option. Action handlers support amendment flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/modes/record.py
@src/slack/handlers/actions.py
@src/domain/channel.py
@src/domain/entities.py
@src/domain/events.py
@src/domain/content.py
@src/slack/blocks/decisions.py
@src/infrastructure/aggregate_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add existing decision detection to RECORD mode</name>
  <files>src/modes/record.py</files>
  <action>
  1. In RecordModeHandler's `_create_decision_preview` method (the method that uses LLM to extract decision and shows the preview), add a check BEFORE returning the preview:

     - Load the aggregate: `aggregate = await load_aggregate(context.channel_id)` (import load_aggregate from infrastructure.aggregate_loader)
     - Call `existing = aggregate.get_entities_in_thread(ThreadTs(context.thread_ts))` to find entities in this thread
     - Filter for decisions only: `existing_decisions = [e for e in existing if e.entity_type == EntityType.DECISION]`
     - If existing_decisions is non-empty, modify the preview to offer amendment instead of new recording

  2. When existing decisions are found, build an amended preview using a new method `_build_amendment_preview_blocks(existing_decision, new_content)`:

     ```python
     def _build_amendment_preview_blocks(self, existing: Entity, new_content: dict) -> list[dict]:
     ```

     This should produce blocks showing:
     - Header: "*Amend Existing Decision*" (not "Draft Decision")
     - Current title and description (briefly)
     - Arrow/divider
     - New proposed content
     - Three buttons:
       - "Amend Decision" (action_id: `confirm_amend_decision`, style: primary)
       - "Record as New" (action_id: `confirm_record_decision` — existing handler)
       - "Cancel" (action_id: `cancel_decision` — existing handler)

     Store the existing entity_id in the ModeResult's confirmation_data so the handler can look it up:
     ```python
     confirmation_data = {
         "action": "amend_decision",
         "existing_entity_id": str(existing.id),
         "content": {... new content ...},
     }
     ```

  3. If multiple decisions exist in the thread, use the most recent one (highest version or last in list — entities are ordered by insertion).

  IMPORTANT: The existing `confirm_record_decision` path must still work for new decisions. This change only adds the amendment option when an existing decision is detected. The "Record as New" button lets users explicitly create a second decision in the same thread if that's what they want.

  IMPORTANT: Import EntityType from src.domain.types if not already imported.
  </action>
  <verify>python -c "from src.modes.record import RecordModeHandler; print('OK')"</verify>
  <done>RECORD mode detects existing decisions in thread and shows amendment preview with Amend/Record as New/Cancel options. Stores existing_entity_id in confirmation_data.</done>
</task>

<task type="auto">
  <name>Task 2: Register confirm_amend_decision handler in actions.py</name>
  <files>src/slack/handlers/actions.py</files>
  <action>
  1. Update the `RECORD_CONFIRM_PATTERN` regex to also match `confirm_amend_decision`:

     ```python
     RECORD_CONFIRM_PATTERN = re.compile(r"^(confirm_record|confirm_amend|edit|cancel)_decision$")
     ```

  2. In the `handle_record_confirm` handler, add a branch for `confirm_amend_decision`:

     ```python
     if action_id == "confirm_amend_decision":
     ```

     This branch must:
     a. Parse the decision content from preview blocks (reuse `_parse_record_mode_preview` or parse from amendment preview blocks — the amendment preview has a different structure, so you may need a `_parse_amend_preview` helper or embed the entity_id in button value)

     BETTER APPROACH: Encode the existing_entity_id in the button's `value` field rather than trying to parse it from blocks. Update the RECORD mode preview builder to set:
     ```python
     {
         "type": "button",
         "text": {"type": "plain_text", "text": "Amend Decision"},
         "style": "primary",
         "action_id": "confirm_amend_decision",
         "value": json.dumps({"entity_id": str(existing.id)}),
     }
     ```

     b. Load aggregate, get existing entity by entity_id from button value
     c. Parse new content from the preview blocks (use `_parse_record_mode_preview` or a variant)
     d. Build DecisionContent from parsed data
     e. Post+pin new ADR message (the amended version) — call `_post_and_pin_adr()`
     f. Call `aggregate.amend_decision(entity_id, user_id, new_content, reason="Amended via thread discussion", new_adr_message_ts=adr_ts)`
     g. Save events: `await save_events(aggregate)`
     h. If old entity had an `adr_message_ts`, update the old pinned message to show it was superseded (add a strikethrough or context block saying "Amended — see updated version")
     i. If entity has a jira_link (CommittedEntity won't be amendable, but check anyway), call jira notify
     j. Replace buttons with confirmation context block
     k. Update dashboard

  3. For the "update old pinned ADR" step (h): Use `client.chat_update()` to prepend a context block to the old ADR message:
     ```python
     {"type": "context", "elements": [{"type": "mrkdwn", "text": "⚠️ _This decision has been amended. See updated version above._"}]}
     ```
     This requires fetching the old message's blocks first via `client.conversations_history(channel, latest=old_ts, inclusive=True, limit=1)` or simply posting an update that adds the warning.

  IMPORTANT: The `amend_decision` method only works on Draft and Proposed entities. If the entity is Approved/Committed, the handler should fall back to suggesting deprecation instead. Show a message: "This decision is already {state}. To change it, deprecate and record a new one."
  </action>
  <verify>python -c "from src.slack.handlers.actions import register_action_handlers; print('OK')"</verify>
  <done>confirm_amend_decision handler works end-to-end: parses entity_id from button value, loads aggregate, amends decision, posts updated ADR, marks old ADR as amended, updates dashboard. Falls back gracefully for non-amendable entity states.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/ -x` passes
- [ ] RECORD mode in a thread with existing decision shows amendment preview
- [ ] RECORD mode in a thread without decisions shows normal preview (unchanged behavior)
- [ ] confirm_amend_decision handler calls aggregate.amend_decision correctly
- [ ] Old ADR message gets "amended" context block
- [ ] New ADR message gets posted and pinned
- [ ] Dashboard updates after amendment
</verification>

<success_criteria>
- RECORD mode detects existing decisions via get_entities_in_thread
- Amendment preview shows current vs proposed content with Amend/Record as New/Cancel buttons
- confirm_amend_decision handler completes full flow (amend, post ADR, update old ADR, dashboard)
- Non-amendable entities (Approved/Committed) get helpful error message
- Existing confirm_record_decision and cancel_decision flows still work
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-decision-lifecycle/09-03-SUMMARY.md`
</output>
