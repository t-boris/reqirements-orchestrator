---
phase: 42-code-modularization
plan: 06
type: execute
wave: 2
depends_on: []
files_modified: [src/slack/handlers/decision_buttons.py, src/slack/handlers/decision_buttons/__init__.py, src/slack/handlers/decision_buttons/approve.py, src/slack/handlers/decision_buttons/change.py, src/slack/handlers/decision_buttons/deprecate.py, src/slack/handlers/decision_buttons/link.py]
autonomous: true
---

<objective>
Split decision_buttons.py (1433 lines) into logical modules by operation.

Purpose: decision_buttons.py handles all decision-related button interactions. Splitting by operation type improves maintainability.

Output: `src/slack/handlers/decision_buttons/` package with 4-5 focused modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-code-modularization/42-CONTEXT.md
@src/slack/handlers/decision_buttons.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create decision_buttons package with approve module</name>
  <files>src/slack/handlers/decision_buttons/__init__.py, src/slack/handlers/decision_buttons/approve.py</files>
  <action>
  1. Create `src/slack/handlers/decision_buttons/` directory

  2. Create `approve.py` with:
     - Decision approval button handlers
     - Approval validation logic
     - Post-approval actions

  3. Create `__init__.py` for re-exports and handler registration
  </action>
  <verify>python -c "from src.slack.handlers.decision_buttons.approve import handle_decision_approve"</verify>
  <done>Decision buttons package created with approve module</done>
</task>

<task type="auto">
  <name>Task 2: Extract change and deprecate handlers</name>
  <files>src/slack/handlers/decision_buttons/change.py, src/slack/handlers/decision_buttons/deprecate.py</files>
  <action>
  1. Create `change.py` with:
     - Decision change button handlers
     - Change modal handlers
     - Edit validation logic

  2. Create `deprecate.py` with:
     - Decision deprecate button handlers
     - Deprecation confirmation
     - Replacement linking
  </action>
  <verify>python -c "from src.slack.handlers.decision_buttons.change import handle_decision_change"</verify>
  <done>Change and deprecate handlers extracted</done>
</task>

<task type="auto">
  <name>Task 3: Extract link handlers and finalize</name>
  <files>src/slack/handlers/decision_buttons/link.py, src/slack/handlers/decision_buttons/__init__.py</files>
  <action>
  1. Create `link.py` with:
     - Decision-Jira linking button handlers
     - Link modal handlers
     - Link validation

  2. Update `__init__.py` with:
     - Re-exports of all handlers
     - `register_decision_handlers()` function

  3. Delete original `src/slack/handlers/decision_buttons.py`
  </action>
  <verify>python -c "from src.slack.handlers.decision_buttons import register_decision_handlers"</verify>
  <done>Decision buttons package complete, original file removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Original `decision_buttons.py` file removed
- [ ] `from src.slack.handlers.decision_buttons import ...` works
- [ ] `python -m pytest tests/ -x -q` passes
- [ ] Each module under 400 lines
</verification>

<success_criteria>
- decision_buttons/ package complete with 4 modules (approve, change, deprecate, link)
- Original decision_buttons.py removed
- All existing imports work
- All tests pass
- Each module <400 lines
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-06-SUMMARY.md`
</output>
