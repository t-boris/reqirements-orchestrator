---
phase: 42-code-modularization
plan: 09
type: execute
wave: 3
depends_on: []
files_modified: [src/db/decision_store.py, src/db/workitem_store.py, src/schemas/structured_draft.py]
autonomous: true
---

<objective>
Split decision_store.py (920), workitem_store.py (852), and structured_draft.py (829) into logical modules.

Purpose: These three files handle data layer concerns and can be split in parallel.

Output: Refactored modules with clear separation of concerns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-code-modularization/42-CONTEXT.md
@src/db/decision_store.py
@src/db/workitem_store.py
@src/schemas/structured_draft.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split decision_store.py</name>
  <files>src/db/decision_store.py, src/db/decision_version_store.py, src/db/decision_link_store.py</files>
  <action>
  1. Keep `decision_store.py` with core Decision CRUD:
     - DecisionStore class
     - create(), get(), update(), deprecate()
     - List and query methods

  2. Create `decision_version_store.py`:
     - DecisionVersionStore class
     - Version history methods
     - get_version_history(), create_version()

  3. Create `decision_link_store.py`:
     - DecisionLinkStore class
     - Link CRUD methods
     - get_links_for_decision(), get_decisions_for_ticket()

  4. Update imports - stores can compose each other
  </action>
  <verify>python -c "from src.db.decision_store import DecisionStore; from src.db.decision_link_store import DecisionLinkStore"</verify>
  <done>decision_store.py split into three focused stores</done>
</task>

<task type="auto">
  <name>Task 2: Split workitem_store.py</name>
  <files>src/db/workitem_store.py, src/db/workitem_queries.py</files>
  <action>
  1. Keep `workitem_store.py` with core CRUD:
     - WorkItemStore class
     - create(), get(), update(), delete()
     - Basic operations

  2. Create `workitem_queries.py`:
     - Complex query methods
     - get_by_channel(), get_pending(), search()
     - Aggregation and reporting queries
  </action>
  <verify>python -c "from src.db.workitem_store import WorkItemStore"</verify>
  <done>workitem_store.py split into store and queries</done>
</task>

<task type="auto">
  <name>Task 3: Split structured_draft.py</name>
  <files>src/schemas/structured_draft.py, src/schemas/draft_mutations.py, src/schemas/draft_validation.py</files>
  <action>
  1. Keep `structured_draft.py` with models:
     - StructuredDraft class
     - DraftItem, DraftKind, DraftScope, DraftLifecycle
     - Basic model definitions

  2. Create `draft_mutations.py`:
     - Mutation methods (split_to_plan, add_items, merge_items, etc.)
     - State transition logic
     - Change log handling

  3. Create `draft_validation.py`:
     - Form-dependent validation
     - Lifecycle validation
     - Field validation helpers
  </action>
  <verify>python -c "from src.schemas.structured_draft import StructuredDraft"</verify>
  <done>structured_draft.py split into models, mutations, validation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All imports work
- [ ] `python -m pytest tests/ -x -q` passes
- [ ] All files under 400 lines
- [ ] No circular imports
</verification>

<success_criteria>
- decision_store split into 3 files (store, version, link)
- workitem_store split into 2 files (store, queries)
- structured_draft split into 3 files (models, mutations, validation)
- All existing imports work
- All tests pass
- Each file <400 lines
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-09-SUMMARY.md`
</output>
