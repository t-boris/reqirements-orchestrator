---
phase: 42-code-modularization
plan: 02
type: execute
wave: 1
depends_on: ["42-01"]
files_modified: [src/slack/handlers/dispatch/task_plan.py, src/slack/handlers/dispatch/question.py, src/slack/handlers/dispatch/jira_ops.py, src/slack/handlers/dispatch/core.py, src/slack/handlers/dispatch/__init__.py]
autonomous: true
---

<objective>
Complete dispatch.py split by extracting task_plan, question, and jira_ops handlers.

Purpose: Finish modularizing dispatch.py so each module has <400 lines and single responsibility.

Output: dispatch/ package complete with 6 modules, original dispatch.py removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-code-modularization/42-01-SUMMARY.md
@src/slack/handlers/dispatch/__init__.py
@src/slack/handlers/dispatch/core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract task_plan handlers</name>
  <files>src/slack/handlers/dispatch/task_plan.py</files>
  <action>
  Move to `task_plan.py`:
  - `_handle_task_plan_created()` (~50 lines)
  - `_handle_task_confirmation()` (~46 lines)
  - `_handle_task_plan_complete()` (~63 lines)
  - `_handle_task_plan_blocked()` (~50 lines)
  - `_handle_task_failed()` (~50 lines)
  - `_handle_task_rejected()` (~remaining)

  All Phase 35 TaskPlan-related dispatch handlers.
  </action>
  <verify>python -c "from src.slack.handlers.dispatch.task_plan import _handle_task_plan_created"</verify>
  <done>TaskPlan handlers extracted</done>
</task>

<task type="auto">
  <name>Task 2: Extract question handlers</name>
  <files>src/slack/handlers/dispatch/question.py</files>
  <action>
  Move to `question.py`:
  - `_handle_question_posted()` (~40 lines)
  - `_handle_budget_exhausted()` (~37 lines)
  - `_post_question_ui()` (~43 lines)
  - `_post_budget_exhausted_ui()` (~42 lines)

  All Phase 36/37 Question Engine dispatch handlers.
  </action>
  <verify>python -c "from src.slack.handlers.dispatch.question import _handle_question_posted"</verify>
  <done>Question handlers extracted</done>
</task>

<task type="auto">
  <name>Task 3: Extract jira_ops handlers and finalize</name>
  <files>src/slack/handlers/dispatch/jira_ops.py, src/slack/handlers/dispatch/__init__.py</files>
  <action>
  1. Move to `jira_ops.py`:
     - `_check_preflight_for_action()` (~73 lines)
     - `_handle_ticket_action()` (~306 lines)
     - `_handle_create_stories()` (~141 lines)
     - `_handle_sync_request()` (~25 lines)
     - `_handle_jira_search()` (~102 lines)
     - `_handle_change_request_preview()` (~25 lines)
     - `_handle_ops_response()` (~85 lines)
     - `UPDATE_EXTRACTION_PROMPT`, `COMMENT_EXTRACTION_PROMPT`

  2. Update `__init__.py` to re-export all public functions from all modules

  3. Update `core.py` _dispatch_result() to import handlers from new modules

  4. Delete original `src/slack/handlers/dispatch.py` file

  5. Verify all imports work
  </action>
  <verify>python -c "from src.slack.handlers.dispatch import dispatch_result; from src.slack.handlers import dispatch"</verify>
  <done>dispatch.py fully modularized into 6 modules, original file removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Original `dispatch.py` file removed
- [ ] `from src.slack.handlers.dispatch import dispatch_result` works
- [ ] `python -m pytest tests/ -x -q` passes
- [ ] Each module in dispatch/ is under 600 lines
- [ ] `wc -l src/slack/handlers/dispatch/*.py` shows all files <600 lines
</verification>

<success_criteria>
- dispatch/ package complete with 6 modules (core, draft, decision, task_plan, question, jira_ops)
- Original dispatch.py removed
- All existing imports work via __init__.py re-exports
- All tests pass
- Each module <600 lines
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-02-SUMMARY.md`
</output>
