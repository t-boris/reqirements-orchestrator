---
phase: 42-code-modularization
plan: 05
type: execute
wave: 2
depends_on: []
files_modified: [src/slack/handlers/multi_ticket.py, src/slack/handlers/multi_ticket/__init__.py, src/slack/handlers/multi_ticket/creation.py, src/slack/handlers/multi_ticket/linking.py, src/slack/handlers/multi_ticket/ui.py]
autonomous: true
---

<objective>
Split multi_ticket.py (1538 lines) into logical modules by flow.

Purpose: multi_ticket.py handles creating multiple tickets from reviews. Splitting by concern improves maintainability.

Output: `src/slack/handlers/multi_ticket/` package with 3-4 focused modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-code-modularization/42-CONTEXT.md
@src/slack/handlers/multi_ticket.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi_ticket package with creation module</name>
  <files>src/slack/handlers/multi_ticket/__init__.py, src/slack/handlers/multi_ticket/creation.py</files>
  <action>
  1. Create `src/slack/handlers/multi_ticket/` directory

  2. Create `creation.py` with:
     - Ticket creation flow logic
     - Story/epic/subtask creation functions
     - Batch ticket creation handlers

  3. Create `__init__.py` for re-exports
  </action>
  <verify>python -c "from src.slack.handlers.multi_ticket.creation import create_tickets"</verify>
  <done>Multi-ticket package created with creation module</done>
</task>

<task type="auto">
  <name>Task 2: Extract linking and UI modules</name>
  <files>src/slack/handlers/multi_ticket/linking.py, src/slack/handlers/multi_ticket/ui.py</files>
  <action>
  1. Create `linking.py` with:
     - Parent-child linking logic
     - Epic-story relationships
     - Link management functions

  2. Create `ui.py` with:
     - Slack block builders for multi-ticket UI
     - Progress cards
     - Result summaries
  </action>
  <verify>python -c "from src.slack.handlers.multi_ticket.linking import link_to_parent"</verify>
  <done>Linking and UI modules extracted</done>
</task>

<task type="auto">
  <name>Task 3: Finalize package and remove original</name>
  <files>src/slack/handlers/multi_ticket/__init__.py</files>
  <action>
  1. Update `__init__.py` to re-export all public functions

  2. Delete original `src/slack/handlers/multi_ticket.py`

  3. Verify imports work
  </action>
  <verify>python -c "from src.slack.handlers import multi_ticket"</verify>
  <done>Multi-ticket package complete, original file removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Original `multi_ticket.py` file removed
- [ ] `from src.slack.handlers.multi_ticket import ...` works
- [ ] `python -m pytest tests/ -x -q` passes
- [ ] Each module under 600 lines
</verification>

<success_criteria>
- multi_ticket/ package complete
- Original multi_ticket.py removed
- All existing imports work
- All tests pass
- Each module <600 lines
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-05-SUMMARY.md`
</output>
