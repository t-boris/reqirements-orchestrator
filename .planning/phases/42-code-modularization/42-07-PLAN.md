---
phase: 42-code-modularization
plan: 07
type: execute
wave: 2
depends_on: []
files_modified: [src/slack/handlers/draft.py, src/slack/handlers/review.py, src/slack/handlers/sync.py]
autonomous: true
---

<objective>
Split draft.py (1288), review.py (1247), and sync.py (1251) into logical modules.

Purpose: These three handler files are similarly sized and can be split in parallel. Each handles a distinct domain.

Output: Three new packages under `src/slack/handlers/` for draft, review, and sync.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-code-modularization/42-CONTEXT.md
@src/slack/handlers/draft.py
@src/slack/handlers/review.py
@src/slack/handlers/sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split draft.py into package</name>
  <files>src/slack/handlers/draft/__init__.py, src/slack/handlers/draft/create.py, src/slack/handlers/draft/transform.py, src/slack/handlers/draft/approve.py</files>
  <action>
  1. Create `src/slack/handlers/draft/` directory

  2. Split by lifecycle stage:
     - `create.py`: Draft creation, initial extraction
     - `transform.py`: DRAFT_TRANSFORM handlers, structural mutations
     - `approve.py`: Draft approval, commit to Jira

  3. Create `__init__.py` with re-exports

  4. Delete original `src/slack/handlers/draft.py`
  </action>
  <verify>python -c "from src.slack.handlers.draft import handle_draft_approval"</verify>
  <done>draft.py split into draft/ package</done>
</task>

<task type="auto">
  <name>Task 2: Split review.py into package</name>
  <files>src/slack/handlers/review/__init__.py, src/slack/handlers/review/start.py, src/slack/handlers/review/continue_.py, src/slack/handlers/review/complete.py</files>
  <action>
  1. Create `src/slack/handlers/review/` directory

  2. Split by review phase:
     - `start.py`: Review initiation, context gathering
     - `continue_.py`: Review continuation, follow-up questions (note underscore to avoid keyword)
     - `complete.py`: Review completion, artifact creation

  3. Create `__init__.py` with re-exports

  4. Delete original `src/slack/handlers/review.py`
  </action>
  <verify>python -c "from src.slack.handlers.review import handle_review_start"</verify>
  <done>review.py split into review/ package</done>
</task>

<task type="auto">
  <name>Task 3: Split sync.py into package</name>
  <files>src/slack/handlers/sync/__init__.py, src/slack/handlers/sync/preflight.py, src/slack/handlers/sync/manual.py, src/slack/handlers/sync/conflict.py</files>
  <action>
  1. Create `src/slack/handlers/sync/` directory

  2. Split by sync type:
     - `preflight.py`: Preflight sync logic
     - `manual.py`: /maro sync command handling
     - `conflict.py`: Conflict detection and resolution UI

  3. Create `__init__.py` with re-exports

  4. Delete original `src/slack/handlers/sync.py`
  </action>
  <verify>python -c "from src.slack.handlers.sync import handle_sync_command"</verify>
  <done>sync.py split into sync/ package</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Original draft.py, review.py, sync.py files removed
- [ ] All imports from new packages work
- [ ] `python -m pytest tests/ -x -q` passes
- [ ] All new modules under 500 lines
</verification>

<success_criteria>
- Three new packages created: draft/, review/, sync/
- Original files removed
- All existing imports work via __init__.py re-exports
- All tests pass
- Each module <500 lines
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-07-SUMMARY.md`
</output>
