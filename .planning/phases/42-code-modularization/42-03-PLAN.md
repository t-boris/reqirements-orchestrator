---
phase: 42-code-modularization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/graph/intent.py, src/graph/intent/__init__.py, src/graph/intent/pre_gates.py, src/graph/intent/mode_classifier.py, src/graph/intent/intent_classifier.py, src/graph/intent/policy.py]
autonomous: true
---

<objective>
Split intent.py (1149 lines) into logical modules by classification stage.

Purpose: intent.py handles the 2-stage classification architecture from Phase 39. Splitting by stage makes the flow clearer and each component testable independently.

Output: `src/graph/intent/` package with 4-5 focused modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-code-modularization/42-CONTEXT.md
@src/graph/intent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create intent package with pre_gates module</name>
  <files>src/graph/intent/__init__.py, src/graph/intent/pre_gates.py</files>
  <action>
  1. Create `src/graph/intent/` directory
  2. Create `pre_gates.py` with Stage 0 pre-gate logic:
     - `_detect_decision_type_hint()` function
     - Any deterministic pre-gate checks (command detection, terminal handling)
     - Pre-gate constants and helper functions
  3. Create `__init__.py` that re-exports main public functions:
     - `classify_intent`
     - `classify_intent_with_context`
     - `classify_intent_v2`
     - `intent_router_node`
     - `get_intent_classifier`
     - `should_use_multi_intent_classification`
  </action>
  <verify>python -c "from src.graph.intent.pre_gates import _detect_decision_type_hint"</verify>
  <done>Intent package created with pre_gates module</done>
</task>

<task type="auto">
  <name>Task 2: Extract mode classifier (Stage 1)</name>
  <files>src/graph/intent/mode_classifier.py</files>
  <action>
  Move to `mode_classifier.py`:
  - Stage 1 mode classification logic
  - `_llm_classify()` function (the main classification prompt)
  - Mode scoring and candidate generation
  - Multi-intent signal detection

  This is the lightweight LLM call that determines SuperMode.
  </action>
  <verify>python -c "from src.graph.intent.mode_classifier import _llm_classify"</verify>
  <done>Mode classifier extracted</done>
</task>

<task type="auto">
  <name>Task 3: Extract intent classifier (Stage 2) and policy</name>
  <files>src/graph/intent/intent_classifier.py, src/graph/intent/policy.py, src/graph/intent/router.py</files>
  <action>
  1. Create `intent_classifier.py` with:
     - `_llm_classify_multi_intent()` function
     - Intent extraction and TaskPlan building
     - `_generate_task_title()`, `_extract_intent_params()`

  2. Create `policy.py` with:
     - Ambiguity policy logic
     - Risk guards
     - Margin-based decision rules

  3. Create `router.py` with:
     - `intent_router_node()` (the graph node)
     - `classify_intent()` main entry point
     - `classify_intent_with_context()`
     - `classify_intent_v2()`
     - `get_intent_classifier()`
     - `should_use_multi_intent_classification()`

  4. Update `__init__.py` to re-export from all modules

  5. Delete original `src/graph/intent.py`
  </action>
  <verify>python -c "from src.graph.intent import classify_intent, intent_router_node, classify_intent_v2"</verify>
  <done>Intent package complete, original file removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Original `intent.py` file removed
- [ ] `from src.graph.intent import classify_intent` works
- [ ] `from src.graph.intent import intent_router_node` works
- [ ] `python -m pytest tests/ -x -q` passes
- [ ] Each module in intent/ is under 400 lines
</verification>

<success_criteria>
- intent/ package complete with 5 modules (pre_gates, mode_classifier, intent_classifier, policy, router)
- Original intent.py removed
- All existing imports work via __init__.py re-exports
- All tests pass
- Each module <400 lines
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-03-SUMMARY.md`
</output>
