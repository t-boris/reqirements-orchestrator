---
phase: 42-code-modularization
plan: 08
type: execute
wave: 2
depends_on: []
files_modified: [src/graph/nodes/extraction.py, src/graph/nodes/decision.py, src/jira/client.py]
autonomous: true
---

<objective>
Split extraction.py (1290), decision.py (961), and jira/client.py (979) into logical modules.

Purpose: These three files handle core logic and can be split in parallel. Each is over 900 lines.

Output: Three new packages or split files with clear responsibilities.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-code-modularization/42-CONTEXT.md
@src/graph/nodes/extraction.py
@src/graph/nodes/decision.py
@src/jira/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split extraction.py into package</name>
  <files>src/graph/nodes/extraction/__init__.py, src/graph/nodes/extraction/draft.py, src/graph/nodes/extraction/decision.py, src/graph/nodes/extraction/review.py</files>
  <action>
  1. Create `src/graph/nodes/extraction/` directory

  2. Split by extraction type:
     - `draft.py`: Draft field extraction (title, problem, AC, etc.)
     - `decision.py`: Decision extraction from conversation
     - `review.py`: Review artifact extraction

  3. Create `__init__.py` with re-exports of main extraction nodes

  4. Delete original `src/graph/nodes/extraction.py`
  </action>
  <verify>python -c "from src.graph.nodes.extraction import extraction_node"</verify>
  <done>extraction.py split into extraction/ package</done>
</task>

<task type="auto">
  <name>Task 2: Split decision.py (graph node) into package</name>
  <files>src/graph/nodes/decision/__init__.py, src/graph/nodes/decision/create.py, src/graph/nodes/decision/update.py, src/graph/nodes/decision/link.py</files>
  <action>
  1. Create `src/graph/nodes/decision/` directory

  2. Split by operation:
     - `create.py`: Decision creation node logic
     - `update.py`: Decision update/deprecate node logic
     - `link.py`: Decision-Jira linking node logic

  3. Create `__init__.py` with re-exports

  4. Delete original `src/graph/nodes/decision.py`
  </action>
  <verify>python -c "from src.graph.nodes.decision import decision_node"</verify>
  <done>decision.py (graph) split into decision/ package</done>
</task>

<task type="auto">
  <name>Task 3: Split jira/client.py into modules</name>
  <files>src/jira/client.py, src/jira/read.py, src/jira/write.py, src/jira/search.py</files>
  <action>
  1. Split client.py by operation type:
     - Keep `client.py` as thin wrapper with JiraService class and basic config
     - `read.py`: get_issue(), get_issue_children(), get_project_info(), etc.
     - `write.py`: create_issue(), update_issue(), transition_issue(), etc.
     - `search.py`: search_issues(), search_duplicates(), JQL helpers

  2. JiraService class remains in client.py but delegates to modules

  3. Update imports throughout codebase (likely via `from src.jira import JiraService`)
  </action>
  <verify>python -c "from src.jira import JiraService"</verify>
  <done>jira/client.py split into read, write, search modules</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Original extraction.py, decision.py files removed
- [ ] client.py reduced to <300 lines (thin wrapper)
- [ ] All imports work
- [ ] `python -m pytest tests/ -x -q` passes
- [ ] All new modules under 400 lines
</verification>

<success_criteria>
- extraction/ and decision/ packages created under graph/nodes/
- jira/ module split into client.py + read.py + write.py + search.py
- All existing imports work
- All tests pass
- Each module <400 lines
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-08-SUMMARY.md`
</output>
