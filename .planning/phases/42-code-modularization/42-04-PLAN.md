---
phase: 42-code-modularization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/slack/handlers/commands.py, src/slack/handlers/commands/__init__.py, src/slack/handlers/commands/debug.py, src/slack/handlers/commands/sync.py, src/slack/handlers/commands/decisions.py, src/slack/handlers/commands/explain.py, src/slack/handlers/commands/help.py]
autonomous: true
---

<objective>
Split commands.py (1650 lines) into logical modules by command group.

Purpose: commands.py handles all /maro slash commands. Splitting by command domain improves maintainability.

Output: `src/slack/handlers/commands/` package with 5-6 focused modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-code-modularization/42-CONTEXT.md
@src/slack/handlers/commands.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands package with debug and sync modules</name>
  <files>src/slack/handlers/commands/__init__.py, src/slack/handlers/commands/debug.py, src/slack/handlers/commands/sync.py</files>
  <action>
  1. Create `src/slack/handlers/commands/` directory

  2. Create `debug.py` with:
     - `/maro debug on/off` handler
     - `/maro debug status` handler
     - `/maro debug state` handler
     - Debug-related helper functions

  3. Create `sync.py` with:
     - `/maro sync` command handler
     - Sync-related UI and logic

  4. Create `__init__.py` that will re-export the main `handle_slash_command()` or `register_commands()` function
  </action>
  <verify>python -c "from src.slack.handlers.commands.debug import handle_debug_command"</verify>
  <done>Commands package created with debug and sync modules</done>
</task>

<task type="auto">
  <name>Task 2: Extract decisions and explain commands</name>
  <files>src/slack/handlers/commands/decisions.py, src/slack/handlers/commands/explain.py</files>
  <action>
  1. Create `decisions.py` with:
     - `/maro decisions` list command
     - `/maro decision show <id>` command
     - `/maro decision change <id>` command
     - `/maro decision deprecate <id>` command
     - `/maro decision enrich <id>` command
     - `/maro decision needs-context` command

  2. Create `explain.py` with:
     - `/maro explain` command handler
     - OPS explain output formatting
  </action>
  <verify>python -c "from src.slack.handlers.commands.decisions import handle_decisions_command"</verify>
  <done>Decisions and explain commands extracted</done>
</task>

<task type="auto">
  <name>Task 3: Create help module and finalize package</name>
  <files>src/slack/handlers/commands/help.py, src/slack/handlers/commands/router.py, src/slack/handlers/commands/__init__.py</files>
  <action>
  1. Create `help.py` with:
     - `/maro help` command
     - Command documentation

  2. Create `router.py` with:
     - Main command router function that dispatches to specific handlers
     - `register_commands()` or similar registration function
     - Parse command text and route to appropriate module

  3. Update `__init__.py` to re-export main entry points

  4. Delete original `src/slack/handlers/commands.py`
  </action>
  <verify>python -c "from src.slack.handlers.commands import register_commands"</verify>
  <done>Commands package complete, original file removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Original `commands.py` file removed
- [ ] `from src.slack.handlers.commands import ...` works
- [ ] `python -m pytest tests/ -x -q` passes
- [ ] Each module in commands/ is under 400 lines
</verification>

<success_criteria>
- commands/ package complete with 6 modules (debug, sync, decisions, explain, help, router)
- Original commands.py removed
- All existing imports work via __init__.py re-exports
- All tests pass
- Each module <400 lines
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-04-SUMMARY.md`
</output>
