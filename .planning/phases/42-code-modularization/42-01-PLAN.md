---
phase: 42-code-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/slack/handlers/dispatch.py, src/slack/handlers/dispatch/__init__.py, src/slack/handlers/dispatch/core.py, src/slack/handlers/dispatch/draft.py, src/slack/handlers/dispatch/decision.py, src/slack/handlers/dispatch/task_plan.py, src/slack/handlers/dispatch/question.py, src/slack/handlers/dispatch/jira_ops.py]
autonomous: true
---

<objective>
Split dispatch.py (2717 lines) into logical modules by action domain.

Purpose: dispatch.py is the largest file and handles too many concerns. Splitting by action type improves maintainability and makes the code navigable.

Output: `src/slack/handlers/dispatch/` package with 6-7 focused modules, all imports preserved via `__init__.py` re-exports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-code-modularization/42-CONTEXT.md
@src/slack/handlers/dispatch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dispatch package structure</name>
  <files>src/slack/handlers/dispatch/__init__.py, src/slack/handlers/dispatch/core.py</files>
  <action>
  1. Create `src/slack/handlers/dispatch/` directory
  2. Create `core.py` with:
     - Imports and constants (lines 1-100)
     - `resolve_attachment_context()`
     - `_extract_update_content()`
     - `_extract_comment_content()`
     - Main `_dispatch_result()` function (the router)
  3. Create `__init__.py` that re-exports `_dispatch_result` as `dispatch_result` and other public functions

  The goal is to keep the main dispatch router in core.py while moving handlers to domain-specific modules.
  </action>
  <verify>python -c "from src.slack.handlers.dispatch import dispatch_result" succeeds</verify>
  <done>Core dispatch module created with main router function</done>
</task>

<task type="auto">
  <name>Task 2: Extract draft-related handlers</name>
  <files>src/slack/handlers/dispatch/draft.py</files>
  <action>
  Move to `draft.py`:
  - `_handle_draft_conflict()` (~60 lines)
  - `_handle_transform_applied()` (~80 lines)
  - Any draft-related helper functions

  Keep imports minimal - only what's needed for these functions.
  Import from `core.py` if shared utilities are needed.
  </action>
  <verify>python -c "from src.slack.handlers.dispatch.draft import _handle_draft_conflict, _handle_transform_applied"</verify>
  <done>Draft handlers extracted to dedicated module</done>
</task>

<task type="auto">
  <name>Task 3: Extract decision-related handlers</name>
  <files>src/slack/handlers/dispatch/decision.py</files>
  <action>
  Move to `decision.py`:
  - `_handle_decision_approval()` (~140 lines)
  - `_link_decision_to_jira()` (~100 lines)
  - `_prompt_decision_link()` (~105 lines)
  - `_handle_expand_decision()` (~70 lines)
  - `DECISION_EXTRACTION_PROMPT` constant

  These all deal with decision entity operations.
  </action>
  <verify>python -c "from src.slack.handlers.dispatch.decision import _handle_decision_approval"</verify>
  <done>Decision handlers extracted to dedicated module</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from src.slack.handlers.dispatch import dispatch_result"` succeeds
- [ ] `python -m pytest tests/ -x -q` passes (no test changes needed)
- [ ] No circular imports
- [ ] dispatch/__init__.py re-exports all public functions for backward compatibility
</verification>

<success_criteria>
- dispatch/ package created with core.py, draft.py, decision.py
- All existing imports from `src.slack.handlers.dispatch` continue to work
- Tests pass without modification
- No behavioral changes
</success_criteria>

<output>
After completion, create `.planning/phases/42-code-modularization/42-01-SUMMARY.md`
</output>
