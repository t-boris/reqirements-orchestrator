---
phase: 35-multi-intent-task-orchestration
plan: 07
type: execute
wave: 4
depends_on: ["35-05", "35-06"]
files_modified: [src/slack/handlers/task_plan.py, src/slack/router.py]
autonomous: true
---

<objective>
Create button handlers for TaskPlan controls with version binding.

Purpose: Handle user interactions with status card buttons (Approve, Reject, Cancel). Version binding prevents stale actions from succeeding.

Output: Button handlers registered with Slack app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-multi-intent-task-orchestration/35-CONTEXT.md
@.planning/phases/35-multi-intent-task-orchestration/35-RESEARCH.md

@src/slack/handlers/duplicates.py
@src/slack/handlers/draft.py
@src/slack/router.py
@src/graph/nodes/task_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskPlan button handlers</name>
  <files>src/slack/handlers/task_plan.py</files>
  <action>
Create handlers for all TaskPlan button actions:

```python
"""Button handlers for TaskPlan interactions.

Phase 35: Multi-Intent Task Orchestration

Handles:
- task_approve: User approves a blocked task
- task_reject: User rejects a blocked task
- task_plan_cancel: User cancels entire plan

All handlers use version binding for idempotency:
- Button value format: "{plan_id}:{task_id}:{version}" or "{plan_id}:{version}"
- Version mismatch → "stale action" message
"""

import logging
from typing import Callable

from slack_sdk.web.async_client import AsyncWebClient

from src.db.connection import get_connection
from src.db.task_plan_store import TaskPlanStore
from src.graph.nodes.task_executor import handle_task_approval
from src.schemas.state import AgentState
from src.schemas.task_plan import TaskPlan, TaskPlanStatus, TaskStatus
from src.slack.blocks.task_plan import build_plan_canceled_message
from src.slack.task_status_updater import TaskStatusUpdater
from src.slack.handlers.core import _run_async

logger = logging.getLogger(__name__)


async def handle_task_approve(
    client: AsyncWebClient,
    body: dict,
    respond: Callable,
) -> None:
    """Handle task approval button click.

    Button value format: "{plan_id}:{task_id}:{version}"
    """
    user_id = body.get("user", {}).get("id")
    action = body.get("actions", [{}])[0]
    value = action.get("value", "")

    parts = value.split(":")
    if len(parts) != 3:
        await respond(text="Invalid action data")
        return

    plan_id, task_id, version_str = parts
    try:
        version = int(version_str)
    except ValueError:
        await respond(text="Invalid version")
        return

    # Load plan and validate
    async with get_connection() as conn:
        store = TaskPlanStore(conn)
        task_plan = await store.get(plan_id)

    if not task_plan:
        await respond(text="Plan not found")
        return

    # Version check
    if task_plan.version != version:
        await respond(
            text="⚠️ This action is outdated. The plan has been updated.",
            response_type="ephemeral",
        )
        # Update the card to show current state
        updater = TaskStatusUpdater(client)
        await updater.update_card(
            task_plan,
            body.get("channel", {}).get("id"),
            event="stale_refresh",
        )
        return

    # Find task
    task = next((t for t in task_plan.tasks if t.task_id == task_id), None)
    if not task:
        await respond(text="Task not found")
        return

    if task.status != TaskStatus.BLOCKED:
        await respond(
            text=f"Task is already {task.status.value}",
            response_type="ephemeral",
        )
        return

    # Process approval
    state = AgentState(
        channel_id=body.get("channel", {}).get("id"),
        thread_ts=task_plan.thread_ts,
        user_id=user_id,
        task_plan=task_plan.model_dump(),
    )

    result = await handle_task_approval(
        state=state,
        plan_id=plan_id,
        task_id=task_id,
        approved=True,
        plan_version=version,
    )

    # Update status card
    updated_plan_data = result.get("task_plan")
    if updated_plan_data:
        updated_plan = TaskPlan.model_validate(updated_plan_data)
        updater = TaskStatusUpdater(client)
        await updater.update_card(
            updated_plan,
            body.get("channel", {}).get("id"),
            event="task_completed",
        )

    logger.info(f"Task {task_id} approved by {user_id}")


async def handle_task_reject(
    client: AsyncWebClient,
    body: dict,
    respond: Callable,
) -> None:
    """Handle task rejection button click.

    Button value format: "{plan_id}:{task_id}:{version}"
    """
    user_id = body.get("user", {}).get("id")
    action = body.get("actions", [{}])[0]
    value = action.get("value", "")

    parts = value.split(":")
    if len(parts) != 3:
        await respond(text="Invalid action data")
        return

    plan_id, task_id, version_str = parts
    try:
        version = int(version_str)
    except ValueError:
        await respond(text="Invalid version")
        return

    # Load plan
    async with get_connection() as conn:
        store = TaskPlanStore(conn)
        task_plan = await store.get(plan_id)

    if not task_plan:
        await respond(text="Plan not found")
        return

    # Version check
    if task_plan.version != version:
        await respond(
            text="⚠️ This action is outdated. The plan has been updated.",
            response_type="ephemeral",
        )
        return

    # Process rejection
    state = AgentState(
        channel_id=body.get("channel", {}).get("id"),
        thread_ts=task_plan.thread_ts,
        user_id=user_id,
        task_plan=task_plan.model_dump(),
    )

    result = await handle_task_approval(
        state=state,
        plan_id=plan_id,
        task_id=task_id,
        approved=False,
        plan_version=version,
    )

    # Update status card
    updated_plan_data = result.get("task_plan")
    if updated_plan_data:
        updated_plan = TaskPlan.model_validate(updated_plan_data)
        updater = TaskStatusUpdater(client)
        await updater.update_card(
            updated_plan,
            body.get("channel", {}).get("id"),
            event="task_blocked",
        )

    await respond(
        text=f"Task rejected. Dependent tasks have been canceled.",
        response_type="ephemeral",
    )
    logger.info(f"Task {task_id} rejected by {user_id}")


async def handle_plan_cancel(
    client: AsyncWebClient,
    body: dict,
    respond: Callable,
) -> None:
    """Handle plan cancellation button click.

    Button value format: "{plan_id}:{version}"
    """
    user_id = body.get("user", {}).get("id")
    action = body.get("actions", [{}])[0]
    value = action.get("value", "")

    parts = value.split(":")
    if len(parts) != 2:
        await respond(text="Invalid action data")
        return

    plan_id, version_str = parts
    try:
        version = int(version_str)
    except ValueError:
        await respond(text="Invalid version")
        return

    # Load and cancel plan
    async with get_connection() as conn:
        store = TaskPlanStore(conn)
        task_plan = await store.get(plan_id)

        if not task_plan:
            await respond(text="Plan not found")
            return

        # Version check
        if task_plan.version != version:
            await respond(
                text="⚠️ This action is outdated. The plan has been updated.",
                response_type="ephemeral",
            )
            return

        # Cancel the plan
        task_plan = await store.cancel_plan(plan_id, user_id)

    # Update status card to show canceled state
    updater = TaskStatusUpdater(client)
    await updater.update_card(
        task_plan,
        body.get("channel", {}).get("id"),
        event="plan_canceled",
    )

    # Post cancellation message
    channel_id = body.get("channel", {}).get("id")
    message = build_plan_canceled_message(task_plan, user_id)
    await client.chat_postMessage(
        channel=channel_id,
        thread_ts=task_plan.thread_ts,
        text=message,
    )

    logger.info(f"Plan {plan_id} canceled by {user_id}")


def register_task_plan_handlers(app) -> None:
    """Register TaskPlan button handlers with Slack app.

    Call from router.py during app setup.
    """
    @app.action("task_approve")
    def on_task_approve(ack, body, client, respond):
        ack()
        _run_async(handle_task_approve(client, body, respond))

    @app.action("task_reject")
    def on_task_reject(ack, body, client, respond):
        ack()
        _run_async(handle_task_reject(client, body, respond))

    @app.action("task_plan_cancel")
    def on_plan_cancel(ack, body, client, respond):
        ack()
        _run_async(handle_plan_cancel(client, body, respond))

    logger.info("Registered TaskPlan button handlers")
```
  </action>
  <verify>python -c "from src.slack.handlers.task_plan import register_task_plan_handlers; print('OK')"</verify>
  <done>TaskPlan button handlers with version binding</done>
</task>

<task type="auto">
  <name>Task 2: Register handlers in router</name>
  <files>src/slack/router.py</files>
  <action>
Add TaskPlan handler registration to router.py:

1. Import the registration function:
```python
from src.slack.handlers.task_plan import register_task_plan_handlers
```

2. Call it in the handler registration section (near other handler registrations):
```python
# Register TaskPlan handlers (Phase 35)
register_task_plan_handlers(app)
```

This follows the pattern used by:
- `register_attachment_handlers(app)`
- `register_preflight_handlers(app)`
- `register_decision_handlers(app)`
  </action>
  <verify>grep -q "register_task_plan_handlers" src/slack/router.py && echo "OK" || echo "Not found"</verify>
  <done>TaskPlan handlers registered in router</done>
</task>

<task type="auto">
  <name>Task 3: Add idempotency helpers</name>
  <files>src/slack/handlers/task_plan.py</files>
  <action>
Add helper functions for common idempotency patterns:

```python
def parse_task_button_value(value: str) -> tuple[str, str, int] | None:
    """Parse task button value: {plan_id}:{task_id}:{version}

    Returns:
        Tuple of (plan_id, task_id, version) or None if invalid
    """
    parts = value.split(":")
    if len(parts) != 3:
        return None
    try:
        return parts[0], parts[1], int(parts[2])
    except ValueError:
        return None


def parse_plan_button_value(value: str) -> tuple[str, int] | None:
    """Parse plan button value: {plan_id}:{version}

    Returns:
        Tuple of (plan_id, version) or None if invalid
    """
    parts = value.split(":")
    if len(parts) != 2:
        return None
    try:
        return parts[0], int(parts[1])
    except ValueError:
        return None


async def validate_task_action(
    plan_id: str,
    task_id: str,
    version: int,
    expected_status: TaskStatus = TaskStatus.BLOCKED,
) -> tuple[TaskPlan | None, Task | None, str | None]:
    """Validate a task action request.

    Returns:
        Tuple of (plan, task, error_message)
        If error_message is not None, action should be rejected.
    """
    async with get_connection() as conn:
        store = TaskPlanStore(conn)
        task_plan = await store.get(plan_id)

    if not task_plan:
        return None, None, "Plan not found"

    if task_plan.version != version:
        return task_plan, None, "This action is outdated. The plan has been updated."

    task = next((t for t in task_plan.tasks if t.task_id == task_id), None)
    if not task:
        return task_plan, None, "Task not found"

    if task.status != expected_status:
        return task_plan, task, f"Task is already {task.status.value}"

    return task_plan, task, None


async def validate_plan_action(
    plan_id: str,
    version: int,
) -> tuple[TaskPlan | None, str | None]:
    """Validate a plan-level action request.

    Returns:
        Tuple of (plan, error_message)
    """
    async with get_connection() as conn:
        store = TaskPlanStore(conn)
        task_plan = await store.get(plan_id)

    if not task_plan:
        return None, "Plan not found"

    if task_plan.version != version:
        return task_plan, "This action is outdated. The plan has been updated."

    if task_plan.status in (TaskPlanStatus.DONE, TaskPlanStatus.CANCELED):
        return task_plan, f"Plan is already {task_plan.status.value}"

    return task_plan, None
```

Refactor handlers to use these helpers for cleaner code.
  </action>
  <verify>python -c "from src.slack.handlers.task_plan import validate_task_action; print('OK')"</verify>
  <done>Idempotency helpers for cleaner handler code</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] task_approve handler works with version check
- [ ] task_reject handler cancels task and dependents
- [ ] task_plan_cancel handler cancels entire plan
- [ ] Stale version returns ephemeral error
- [ ] Handlers registered in router.py
- [ ] Status card updates after each action
</verification>

<success_criteria>
- All button handlers implement version binding
- Stale actions rejected with user-friendly message
- Status card updates after approval/rejection/cancel
- Handlers follow existing patterns (ack, _run_async)
</success_criteria>

<output>
After completion, create `.planning/phases/35-multi-intent-task-orchestration/35-07-SUMMARY.md`
</output>
