llm:
  model: gpt-4o-mini
  anthropic_model: claude-3-haiku-20240307

store:
  type: postgres
  postgres:
    dsn: postgres://maro:maro@postgres:5432/maro?sslmode=disable

extractors:
  documents:
    embeddings:
      enabled: true
      service: openai
  messages:
    summarizer:
      enabled: true
    entities:
      enabled: true
    intent:
      enabled: true

server:
  host: 0.0.0.0
  port: 8000

log:
  level: info
